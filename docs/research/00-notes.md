# Notes for HARNESS_V2 (running, per report)

## 04 Prime (done)
- Prime = hard fork of pi-mono (Zechner). TUI/provider/loop inherited; Prime's own: kernel, daemon, always-on features.
- 149 commits since Aug 6 (v0.7.1→0.9.1). IPython GONE → `python -m rlm.repl` JSON-lines subprocess. Still the ONLY tool ("ipython", {code}). RLM-1 TODOs deleted → kernel is the product.
- Daemon: TUI→supervisor→worker per session→python kernel; socket $TMPDIR/prime-agent-<uid>/daemon.sock; identity = pid + start time (#879 TZ bug). Sessions JSONL tree with parentId; kernel-state.dill.
- Verdict: attach/detach idea YES; 33k-line daemon machinery NO. One process under systemd Restart=always.
- Provider layer: 13.7k hand-written + 22k generated catalog; 9 real API impls. Copy types.ts + anthropic.ts + openai-completions.ts (~2.5k lines) — openai-completions covers Ollama/LM Studio via baseUrl + compat flags.
- Claude Code OAuth client ID still present (utils/oauth/anthropic.ts:27-28).
- Steal: cron-jobs.ts (once/cron/interval; steer vs follow_up; deferred when busy; heartbeat = cron job), goals.ts (objective ≤4000 chars + budgets, survives compaction), autonomous.ts budgets (3 continuations, 12 turns, 80k tokens, 30 min; verifier gates = shell cmds w/ 3 retries), billing-vs-context accounting (agent-session.ts:1032-1040), compaction tells summarizer what survives, harness state JSON kinds prompt/memory/skill/subagent + refinements.jsonl, extension hooks w/ block result (28 events), doctor --fix / shutdown --force.
- Avoid: no permission surface (0 hits), single tool, god objects (agent-session.ts 11,948; interactive-mode.ts 10,143; daemon-mode.ts 7,515), 3 procs/session, analytics on by default.
- REPL verdict: not in v1; keep bash()-returns-handle shape with a stateless python tool.
- TUI: custom differential renderer, Component = render(width)->string[]; editor.ts 2,390; Kitty keys; mermaid→unicode 77 lines. Not installed on Mac.
- Hand-written non-test TS ≈ 180k.

## 07 News since August (done)
- "OpenClaw 2.0" = official nickname of v2026.8.1 (docs/releases/2026.8.1.md title "v2026.8.1 (AKA OpenClaw 2.0)"); blog "OpenClaw 2.0, Accidentally" 2026-08-30 (Hannes Rudolph): 933 (blog) / 987 (notes) contributors, 569 first-timers, ~16,977 PRs (~half of all PRs ever), after 106 releases in 230 days then 7-week freeze. Published 2026-08-31. v2026.8.2 next day (784 PRs, 134 contributors). Beta mislabeled as v2026.9.1-beta.1.
- 8.1 headline: rebuilt Control UI (ChatGPT-like), sessions/transcripts moved to SQLite (breaking; downgrade needs restore), per-session permission modes (read-only/guarded/workspace/full), Secret Store (not encrypted at rest), plugin capability review, ClawHub installs need full commit SHA, untrusted content marked, network policy NAT64 blocks, sandbox OFF by default. Startup "575 ms from 1.6 s" (third-party, unverified).
- Issues: 3,683 open; 4,803 opened in last 30 days (~160/day); doctor 460, systemd 233, stuck 211, hang 139, "update failed" 64. Stars 388,622.
- Representative: #134993 gateway pegs CPU core (fs discovery busy loop) after 8.1; #136311 19 GB orphaned memory-reindex temp DBs; #135084 system-prompt stage blocked ~285 s by memory provenance repair; #135038 gateway unstartable after 8.1; #134619 "Upgrade to 8.1 completely breaks existing installation"; #135502 Signal omits plugin-command authorization; #136113 CLI backend loses ~44% of responses when stdout > ~50 KB; #136290 restart recovery executes stale ops hours later; #135347 memory reindex corrupts shared DB.
- ClawStat.us exists to say whether a release is safe; 2026-09-02 advice: stay on 2026.7.1-2.
- HN "OpenClaw 2.0, Accidentally" 148 pts/173 comments: "all this complexity leads to stuff breaking with every update"; "vibecoded mess"; "569 first time contributors yea i shall continue to not use openclaw". The Register 2026-08-31: "pours glitter on slow-burning security dumpster fire".
- Security: no new CVE/advisory Aug–Sep (latest batch 2026-06-30). Prior: CVE-2026-25253 (8.8), -32922 (9.9), -44112 (9.6), -44115, -44118, -44113. ClawHavoc Feb: 1,184 malicious skills (Antiy). Manifold 2026-09-02: 23 scope-squatted plugins under @openclaw/@clawhub (June finding).
- Hermes: 239,895 stars; 12,965 open issues (3.5x OpenClaw); 5,018 opened in 30 days; 6,719 commits since Aug 3 by 3,197 contributors all-time. 8 releases in 29 days (v0.20.0 "Herald" 08-03 → v0.21.0 "Pantheon" 08-31: Bot Mode named agents, live subagent orchestration, MCP dashboard, agent drives desktop browser directly). TUI = Ink + React 19, first commit 2026-04-02, package hermes-tui v0.0.1 private, 89 commits in Aug. 120 open issues with "TUI" in title opened in last month: #99388 SIGSEGV during streaming (V8 use-after-free), #100167 heartbeat dead-timeout drops slow-model generations, #99277 stray 'l' typed on reattach, #99032 submit silently sends paste placeholder. Python core NOT being replaced: 5,074 .py; tui_gateway all Python; cli.py 1,047,067 bytes; AGENTS.md 96 KB; cli-config.yaml.example 113 KB; native/ = one C FTS5 CJK tokenizer; no Cargo.toml.
- OpenCode: repo moved to anomalyco/opencode; 203,206 stars; v1.18.26 on 2026-09-01; ~10 releases in 21 days, all small provider/auth fixes. TUI: NOT Go/Bubble Tea (deleted 2025-11-02 commit f68374ad22 "DELETE GO BUBBLETEA CRAP HOORAY"; zero .go files) → TypeScript + SolidJS on OpenTUI (@opentui/core 0.4.5) on Bun 1.3.14. Desktop app = Electron (beta, v2 default since 1.18.0 July). CONTEXT.md = 32 KB design doc "OpenCode Session Runtime". Built on Effect throughout. Remote attach over Tailscale works but #6835 desktop crash after attach.
- Prime: 19,654 stars, 78 open issues; created 2026-05-08, public 2026-08-05; v0.8.0 hardened OAuth binding; v0.8.1 RLM depth 1→2; v0.9.0 kernel snapshot atomic writes. "RLM-1" does not exist — RLM is an architecture; Prime Intellect models are INTELLECT-3. Paper arXiv:2608.23552. HN 249 pts; criticisms: token cost, 10k-line files, installer wrote into Homebrew dir.
- Consensus: OpenClaw = orgs of agents, breadth; Hermes = one agent that grows; biggest complaint for both = self-hosting difficulty. OpenCode wins TUI craft outright.

## 01 OpenClaw architecture (done)
- Sizes (non-test .ts): src 1.88M, extensions 980k (151 plugins w/ manifest), ui 338k (Lit 3 + Vite, 58 routes), packages 98k. CHANGELOG 35,588 lines, 723 mention "startup".
- Startup chain: openclaw.mjs → src/entry.ts (339 lines of dynamic imports) → 4 workaround modules: esm-resolve-fast-path (326 subpath exports, "~15k module edges", "multiple seconds"), compile-cache (V8 cache + respawn), respawn (restarts Node to silence ExperimentalWarning for node:sqlite, again for CA certs), version-fast-path. Startup tracer with ~40 phases. Est modules: --version ~134; gateway 2,441–7,918 modules (499k–1.81M lines).
- Cron does NOT start at boot (lazy on first use). BOOT.md self-check via throwaway session (src/gateway/boot.ts:104-158) — keep.
- src/daemon = 15k lines of service INSTALL (launchd/systemd/schtasks), not a daemon.
- agent-core now 6,944 lines (agent-loop.ts 1,776). runLoop per turn: steering read → outer while for follow-ups, inner while for tool calls → inject pending → stream → on toolUse run batch, taint → loop detector (10/20/30 fixed, one-shot recovery msg, 2nd critical = terminate) → prepareNextTurn (model swap) → shouldStopAfterTurn → drain follow-ups. STILL NO max-turn cap. Outer runner (embedded-agent-runner, 67k lines): attempts = 24 + 8*profiles clamped 32..160; retry budget refunds progress continuation.
- Queue modes messages.queue.mode: steer|followup|collect|interrupt, cap 20, drop summarize|old|new.
- tool-call-repair 3,564 lines: rescues text tool calls (legacy [END_TOOL_REQUEST], Harmony <|channel|> markers), allowlist + maxPayloadBytes → most useful piece for local models.
- Signal ext: 12,481 lines, deps ws+zod. Spawns `signal-cli daemon --http`, JSON-RPC POST /api/v1/rpc, health /api/v1/check, SSE /api/v1/events, reconnect 1–10s jitter; DM policy pairing(default)/allowlist/open/disabled; chunk 4,000 chars; attachments capped by mediaMaxBytes; group sends fan out per member so no blind retry. ≈10 behaviours ≈ 500–700 lines.
- Cron: 33,606 lines; kinds at/every/cron/stream; payloads systemEvent/agentTurn/script; delivery none/announce/webhook; SQLite tables cron_jobs, cron_run_receipts...; parser croner; single setTimeout clamped 60s; backoff 30s/60s/5m/15m/60m; auto-disable after 10 failures w/ user warning; one `cron` tool with actions. Rewrite ≈ few hundred lines on croner.
- Sessions: key agent:<id>:<mainKey>; all DMs share main session; transcripts as rows transcript_events(session_id, seq, event_json); reset none|daily|idle.
- System prompt: buildAgentSystemPrompt 1,610-line file; SYSTEM_PROMPT_CACHE_BOUNDARY splits stable prefix / volatile suffix (~45 sections). Workspace files AGENTS.md, SOUL.md, IDENTITY.md, USER.md, BOOTSTRAP.md, MEMORY.md; 20k chars/file, 60k total. Est 3–3.5k tokens bare, 7–8k with templates, skills catalog +18k chars.
- Memory: memory-core (37k lines) FTS5 + sqlite-vec hybrid over MEMORY.md/USER.md/memory/**.md; chunks 400 tok/80 overlap, 6 results, min 0.35, 0.7 vec/0.3 text, 30-day decay; recall tool-only via memory_search (no auto-injection). Compaction reserveTokens 16,384, keepRecent 20,000.
- Plugins: manifest openclaw.plugin.json; API has 62 register* methods; 42 hooks (hasHooks gate); 151 manifests: 34 onStartup, 27 channels, 79 providers, 2 memory. In-process, NOT sandboxed (docs/plugins/architecture.md:426-429). src/plugins 118k + src/plugin-sdk 54k.
- Deps: root 65 deps + 60 dev; ~1,650 lockfile packages; heavy: playwright-core, koffi (FFI), node-pty, web-tree-sitter, quickjs-wasi, photon-node, rastermill, clawpdf, @trycua/cua-driver, fs-safe (8 binaries), typescript at runtime, pi-tui, express+undici, lancedb+arrow, baileys. node:sqlite (no better-sqlite3) — good.
- SQLite: 121 state tables + 39 agent tables (schema v15/v19) — single user needs ~11.
- 20% rewrite estimate: ~4,000–5,500 lines TS + UI 800–1,200 (table of pieces).
- Steal (13): pure loop w/ callback seams; two queues four modes; fixed loop thresholds; retry refund; text→tool-call repair; cron backoff+auto-disable+60s-clamped timer; signal-cli HTTP daemon; pairing default; prompt cache boundary; workspace md w/ caps; FTS5 memory tool-only; startup trace + BOOT.md; node:sqlite.
- Don't (12): respawn to set Node flag; 326 exports + resolver hook; compile-cache dance; no turn cap + 160 attempts; 1.88M lines; 160 tables; in-process plugins 62 registrations/42 hooks; 1,610-line prompt builder ~45 sections; 27 channels+79 providers in one process; native/FFI deps; tests > code (3,367-line test-utils file); 94k-line config.

## 02 OpenClaw failure modes (done)
- Screenshot line-by-line: (1) SERVICE_DEFINITION_UNKNOWN [unsafe-permissions] = `mode & 0o022` check on ~/.config/systemd/user or nearest ancestor (systemd-definition-mutation.ts:151-152); Debian/Ubuntu private group + 002 umask → 775 dirs trip it; NO override; created Aug 28 (shipped 8.1). (2) "updated install refresh fail" = updater spawns NEW version `node dist/index.js gateway install --force` (60s timeout) which hits the check; then DEFINITION_DENIAL → preserveDefinition, restarts with OLD unit → "restarted and verified" while still on /usr/bin/node. (3) ERR_MODULE_NOT_FOUND doctor-health-*.js = old updater ran Doctor in-process after npm swapped dist/ (hashed chunks gone); known bug class (tsdown.config.ts:378-379 stable-filename list; "Preload execution and recovery before the package swap"); fixed Aug 30 (f346846aa85, fresh-process Doctor) — JT's updater predated fix; recurs in 8.3 changelog. (4) "23% more sass" tagline removed Aug 30 → proves old updater. (5) Node skew: installer uses nodesource /usr/bin/node in unit; user uses nvm → normal state. (6) "package manager owner is unknown" = runs `npm root -g` with shell PATH (nvm) ≠ ~/.local/lib/node_modules → null → refuse; refusal added Aug 31 (38ecc4173b2); old code defaulted to npm → run N installed the rule that refused run N+1.
- Root causes: A) swaps code under running processes (773–1,830 dynamic import sites); B) tries to prove host facts a user-space tool can't (26 distinct refuse/block messages; 8 reasons enum); C) new refusals ship without migration notes; D) ops layer 317k lines vs 67k loop = 4.7:1; 22 OPENCLAW_UPDATE_* env vars, 476 OPENCLAW_* total; Aug: 11,082 non-merge commits, 377 authors; 538 touched update/daemon/doctor; 985 commits in 2 days since 8.2 tag.
- Stuck: per-session lane + global lane (main/subagent/cron/nested) + activeRunsByKey; nothing on disk as lease; default turn timeout 48h; lane wait no timeout; abort only detaches tool ("JavaScript cannot cancel a running promise", agent-tools.abort.ts:20-27); runCommandWithTimeout no default deadline; SIGUSR1 restarts skip finally → stale task ids block work; 5 competing timers (2 min warn, 5 min stuck abort, 10 min stale takeover, 15 min blocked-tool floor pinned by #88870/#96168, 30 min orphan sweep, 48h turn); steered msgs never refresh liveness; follow-up queue memory-only, dropped on cap/restart; pending-tool drain gives up after 30s "to avoid session deadlock". Outbound delivery queue IS persisted (SQLite, replay, 5 retries 5s–10m) — copy that.
- Changelog fix buckets 8.1/8.2/8.3: total 608 fixes; agent loop/sessions 155; UI 117; channels 91; update/install/doctor 75 (29% of 8.2); gateway lifecycle 38; providers 54; security 31. Keywords: recovery 105, restart 80, timeout 20, stall 18, lock 13. Bun 1.4 breakage "restored" in both 8.2 and 8.3.
- Slow: 3,536 of 8,703 src files statically imported at gateway start (41%), 6,436 with lazy (74%); 238 distinct tables incl plugins; 246 files open SQLite; 42 hooks / ~20 checkpoints per turn; prompt builder ~45k chars union; workspace files up to 60k chars (~15k tokens) re-read per turn (mtime guard); 79 setInterval, 708 setTimeout; idle intervals: 30s maintenance, 60s health, 25s WS keepalive, 15s SSE heartbeat, six 60s reconcilers, 5s terminal reassert, 25 ms desktop poll; 1 ms stream pump.
- Largest files: ui/src/i18n/locales/en.ts 6,840; update-managed-service-handoff.ts 1,984 (one file for handing an update between processes); GatewayModels.swift 26,220. Top-level config keys 43. Doctor 254 files / 70,979 lines.
- Ten rules: one file; never update from inside; versioned dirs + symlink flip; one runtime one path; hygiene warns never blocks; know own path; no new refusals w/o escape; one liveness clock per turn + kill process group; default subprocess timeout + process group; user queues on disk. Plus: keep ops code smaller than loop.

## 03 Hermes (done)
- 6,440 commits in 28 days (230/day), 870 authors, Teknium 30.6%; fix:feat 5:1; 7 releases; Python core grew 31%. Desktop app 25.7% of commits. run_conversation() now 1996→9,244 = ~7,250 lines one function. gateway/run.py 34,561 lines, GatewayRunner class 412 methods. cli.py 22,411.
- Key changes: Bot Mode/group chats (desktop); managed llama.cpp local runtime (Sep 1, hermes_cli/local_runtime/); core-tool deferral 19 tools default rest behind tool_search (schemas 13.4k→6.9k tokens); liveness watchdogs after gateway deadlocked ~30h with live PID; one shared SessionDB writer (12 sites opened writers, WAL races, 11+ incidents); agent drives in-app browser (old cua_browser removed); cron durable (scheduler.py 4,419→8,585); context anchored on provider usage.prompt_tokens (replaces chars/4).
- Interface stack: classic CLI = prompt_toolkit REPL default (22k); TUI = React 19 on FORKED Ink (31,266-line fork) ↔ newline JSON-RPC over stdio ↔ python -m tui_gateway.entry (server.py 18,495 lines; 21 thread spawn sites, 31 locks, fixed 118 times in 28 days); optional compute_host child to dodge GIL (route table turn-path/run-concurrent/idle-gated). Web chat = xterm.js ↔ WS /api/pty ↔ uvicorn ↔ PTY running `hermes --tui` (Node) ↔ stdio ↔ Python + second WS back = 4 processes, 3 transports per message. Desktop = Electron spawns `hermes serve`, WS. Messaging gateway = separate process + control socket.
- Why glitchy (10): pipe is failure domain (109 _emit sites, per-token events, stdout write fail = exit, crash log exists); _stdin_recovery.py hack (children inherit fd 0, O_NONBLOCK on shared description → readline '' → dies; 10 recoveries/min cap); crash budgets everywhere (3 respawns/60s; compute host 3/5min); bounded replay ring 512 events/64 sessions, epoch added; xterm-in-browser can't redraw; GIL child + two schedulers ("race" 125, "stale" 125, "orphan" 155 mentions in tui_gateway); forked Ink w/ forceRedraw, flickers diag, resizeCoalescer for "flickering remount storm", FPS overlay; threads+locks; `hermes --tui` may run esbuild at launch; surface count (2 TUIs + 17-route web + Electron + 20 platforms) → 1,188 interface fix commits in 28 days.
- Signal: signal-cli HTTP daemon (Hermes never spawns it; default 127.0.0.1:8080); SSE receive; JSON-RPC send/sendTyping/sendReaction/getAttachment; manual `signal-cli link -n "HermesAgent"`; allowlist via env vars SIGNAL_ALLOWED_USERS etc; unknown sender → pairing code if no allowlist; groups "group:<id>" + SIGNAL_REQUIRE_MENTION; attachments base64 100 MB cap, ffmpeg remux voice; token bucket 50 tokens/1 per 4s; markdown→Signal textStyles w/ UTF-16 offsets; 8,000-char split; SSE reconnect 2–60s, health probe 30s, forced reconnect after 120s silence; echo filter; NO streaming/editing; no receipts.
- Cron: jobs.json + file lock; output md per run; executions.db (claimed/running/completed/failed/unknown w/ owner pid+start); tick every 60s under .tick.lock; in-flight registry + stale sweep; catch-up grace ≤2h then fast-forward; timeout 600s inactivity; ESTOP file (`hermes pause`); preflight validates delivery creds and (HEAD) failure_deliver; failure_streak; incidents keyed (job_id, error signature); notepad 16 KB/value 64 KB/job; monitor mode hashes script output, LLM only on change; lifecycle_guard rejects jobs containing `hermes gateway restart` (10s SIGTERM loop). 17,898 lines total.
- Memory/context: verify-on-stop now OFF by default ("more noise than signal"); micro-compaction still off; context anchored on usage.prompt_tokens; MEMORY.md + USER.md + SOUL.md (identity, Aug 30); MemoryManager allows one external memory plugin; review fork = daemon thread replaying convo in forked AIAgent w/ tool whitelist (memory+skill tools), on by default, separate model configurable, deferred when local GPU busy (review_idle_queue.py), prompt pushes "CLASS-LEVEL skills"; skills = dir with SKILL.md + YAML frontmatter, 13 bundled categories; FTS5 session search tool; new guards: deadline.py (one primitive replacing 6), turn_liveness.py, empty_response_guard.py ($2.33 for empty completions), repetition_guard.py, native_compaction.py, prompt_cache_boundary.py, side_question.py (/btw).
- Local models: 39 declarative provider profiles; LM Studio first-class (localhost:1234/v1); Ollama/vLLM/llama.cpp = custom + base_url; ollama_num_ctx → options.num_ctx; think=false only for Ollama-shaped URLs; LM Studio reasoning clamp; 3s probe of Ollama /api/show prefers Modelfile num_ctx over GGUF max; reconcile cached ctx against live server. Tool calling relies on server-native tool_calls — NO text→tool-call parser in loop (only strips stray <tool_call> blocks; ported from openclaw #67318). <think> extraction.
- Ops lessons table (13): restart_loop_guard (3 restarts/300s gap, fails open, disables the resume not the service); startup_watchdog (stdlib OS thread armed at entry, os._exit(75) after 300s unless progress leases); shutdown_watchdog (OS thread, 30s heartbeat file, 3 missed = exit); session_stall (notify once at 300s, one progress clock); turn_lease (per resolved session id, 5s wait, reject on timeout); delivery_ledger (row before send, pending→attempting→delivered/failed, dead-owner rows resent w/ "may be duplicate" marker, 3 attempts/24h); session_db_recovery (single-flight reopen 1–60s backoff); lifecycle_ledger (running/exited sentinel → detect unclean death); drain_control (file marker w/ boot-id epoch + 1h expiry after orphan markers parked gateways 3 days); scale_to_zero (own idle decision); memory_monitor (never wired!); deadline.py; lifecycle_guard; state registry one writer per DB.
- Avoid: screen/state split by stdio; forking a TUI framework; terminal-in-browser; GIL child; giant files; too many surfaces; verify-on-stop nag; micro-compaction cache-breaking; config split env+YAML; docs for nonexistent keys; 39 providers; cron sprawl (3 SQLite files + json + locks); multiple write paths w/ separate validation.

## 05 OpenCode (done)
- Measured on Mac (installed v1.18.19): `--version` median 0.30 s warm (1.81 s cold); binary 137 MiB single Bun-compiled arm64 exe with web UI + tree-sitter worker embedded; `opencode serve` idle RSS ~403 MB; data dir 543 MB (330 MB legacy JSON storage, 109 MB LSP bin, 72 MB snapshots); opencode.db SQLite 20 tables; auth.json 0600 plain JSON. Lockfile 3,233 packages; core 99 runtime deps (19 @ai-sdk/*); Effect in 227/357 core files; 251 test files.
- LOC no tests: core 81,173; packages/core 32,961; tui 27,055; app (web) 138,696; ui 33,519; session-ui 20,295; sdk 30,087 generated; server 1,682; protocol 1,582; plugin 1,612. Prompt text: 37 .txt files, 1,832 lines.
- Architecture: ONE process containing HTTP API (Effect HttpApi, 188 endpoints); SSE /event stream (heartbeat 10s) of every state change; WS only for PTY; /doc OpenAPI → generated SDK; SQLite bun:sqlite + drizzle WAL, 30 migrations. Plain `opencode` opens NO port: server runs in a Bun Worker, TUI fetch = postMessage RPC to in-memory Server.app.fetch (cli/cmd/tui.ts:24-50). `opencode serve --port --hostname --mdns --cors` + `opencode attach <url> --password`; Basic auth from OPENCODE_SERVER_PASSWORD (no password = no auth); default bind 127.0.0.1:4096. Multi-client: each SSE subscriber own queue; x-opencode-directory header. Desktop = Electron sidecar with random password. Web app embedded, served at /*.
- 10 UX decisions: (1) server in worker, no socket wait; (2) first frame immediately, spinner only after 500 ms held ≥3 s; (3) streaming as field deltas message.part.delta {messageID, partID, field, delta} into Solid store, 16 ms batching; (4) native <markdown>/<diff> widgets, tree-sitter in worker, "layout never shifts"; (5) inline permission prompt (not modal) Allow once/Allow always/Reject, Esc rejects, reject w/ feedback text reaches model, "always" shows exact patterns, auto mode for unattended; (6) one command registry {title, slashName, aliases, keybind, category} drives palette + / autocomplete + keybind; (7) leader ctrl+x, tab cycles agents (build/plan), f2 models, ctrl+t variants, which-key panel; (8) /undo restores files from shadow git repo, /redo; (9) `!` shell mode + `@` fuzzy mentions in same box; (10) sessions first-class: fuzzy list, slots <leader>1..9, pin/rename/fork/timeline; sidebar shows context tokens, cost, todos, LSP/MCP status. 33 JSON themes; 5s toasts; $EDITOR; image paste; mouse on; frecency file picker.
- 31 built-in slash commands (/new /sessions /workspaces /models /agents /mcps /variants /connect /org /status /debug /themes /help /exit /share /unshare /rename /timeline /fork /compact /undo /redo /timestamps /thinking /copy /export /editor /skills /warp /move /diff) + server /init /review + user command/*.md ($1..$9, $ARGUMENTS, !`cmd`, @file) + MCP prompts.
- Tools (17 defined, 13–14 visible): bash (2 min timeout; 2,000-line/50 KB cap; spill to file 7 days), read, glob, grep, edit, write, apply_patch (GPT-5 only), task (subagent, background:true), webfetch, websearch, todowrite, skill, question (structured Qs), invalid (catches malformed calls), lsp, plan_exit, execute (code mode). Descriptions in .txt next to code.
- Permissions: {key: ask|allow|deny | {pattern: action}}; last matching rule wins; default ask; merge order builtin agent defaults → user config → agent block; per-agent (plan denies edits; explore read-only); ask flow via Deferred + permission.asked event + POST reply once|always|reject(+message → PermissionCorrectedError reaches model); "always" is in-memory only; doom_loop ask when last N calls identical; NO OS sandbox; external_directory fence; plugins can pre-decide.
- Local models: Vercel AI SDK 6, @ai-sdk/openai-compatible with baseURL; models.dev catalog cached; NO Ollama/LM Studio-specific code; tool_call:false stored but never enforced; docs: raise Ollama num_ctx 16–32k if tool calls fail.
- Roles: agents as .md with frontmatter (description, mode, model, temperature, tools, permission, steps...) in ~/.config/opencode/agent/ or .opencode/; instruction files AGENTS.md/CLAUDE.md/CONTEXT.md walk-up; skills from .claude/skills, .agents/skills, .opencode/skills SKILL.md; NO memory subsystem at all. Plugins: ~20 hooks (tool.execute.before/after, permission.ask, chat.message, ...). `opencode run --format json --session --attach` = headless hook.
- Weight verdict: "OpenCode is not light; it feels light because the client is thin and never waits." Essential core ≈ 15–18k LOC inside 81k package, wrapped by ~250k of clients/codegen/platform.
- Copy spec (7 items): core lib w/ in-memory fetch + optional TCP; SSE with part deltas; SQLite 4 tables; commands defined once; 12 tools; permissions w/ once/always/reject+feedback + doom-loop + add real sandbox; roles as agent md + AGENTS.md + add durable memory; one OpenAI-compatible client honoring tool_call:false; UX thresholds; snapshot before every turn.

## 06 Atomic + field survey (done)
- "Atomic Agent" = AtomicBot-ai/atomic-agent (TypeScript, 2.5k stars, v0.5.4 2026-09-01, Node ≥25.7, GBNF-constrained tool calls, SQLite memory, MCP, cron/webhooks/reminders, `atomic-agent serve` OpenAI-shaped API; Telegram ONLY; no Signal, no web UI; "Developer preview"). Same org ships atomic-hermes (Hermes fork, PolyForm NC, stale) and atomicbot ("fastest way to run OpenClaw"). Local models: managed llama.cpp "TurboQuant" fork, external llama-server, Ollama & LM Studio presets — but these landed 2026-08-07→08-31; README: "Very small models emit malformed tool calls more often." Bugs: #307 TUI V8 heap OOM after ~11h, #121 OOM in 25+ tool-call sessions, #300 installer 404 Intel Macs, #103 SSE tool calls w/o index merged wrong. Alt reading: Eigenwise/atomic-agents Python lib (Instructor+Pydantic, 6.2k stars) = library not app; #282 open re OpenAI-compat JSON mode.
- Verdict: "too broken" fair; "no local models" outdated; "too lightweight" fits the Python lib; fails interface requirement (Telegram only).
- BIG: ZeroClaw (Rust, 32,712 stars, MIT/Apache, v0.8.4 2026-08-02, 262 commits/49 contributors in release, release tarballs 24.8–29.2 MB, aarch64 build): Signal + cron ("SOPs" w/ cron & event triggers) + web dashboard (chat, memory, config, cron, tool inspection) + Ollama/any OpenAI-compatible + fallback chains + approval gates + OS sandboxes (Landlock/Bubblewrap/Seatbelt/Docker) + tool receipts. Signal channel = `signal-cli daemon --http`, SSE /api/v1/events in, JSON-RPC /api/v1/rpc out, config http_url/account/group_id/allowed_from/ignore_attachments; Channel trait = send/listen/health/typing. Provider config [providers.models.<alias>], default_model, fallback_providers = ["claude","local"]. Onboarding rewrites localhost→host.docker.internal. "3.4 MB binary / <5 MB RAM" claims NOT verified.
- Moltis (Rust ~270k LOC / 59 crates, 2,841 stars, MIT, release 20260902.01): Telegram/WhatsApp/Signal/Discord/Teams/Slack/Matrix/Nostr + web UI (WS) + PWA push + cron + CalDAV + email; SQLite vector+FTS memory; Docker/Podman/Apple Container/WASM sandboxes; XChaCha20 at rest; passkeys. macOS bundle ~128 MB, Linux tarball ~66 MB.
- IronClaw (nearai, Rust, 12.6k): no Signal; Routines engine; WASM capability-based sandbox — best security idea.
- Claw family: nanoclaw (TS, 30.7k, containers, no Signal), picoclaw (Go, 29.9k, no Signal), nullclaw (Zig, 8k, claimed 678 KB), microclaw (Rust, 731, Signal incl., 14 platforms, Ollama, scheduled tasks). Name collisions rampant.
- Coding agents: goose (Rust, 53.8k) recipe YAML + `goose schedule add --cron --recipe-source` (two schedulers coexist, fresh Agent per run); codex (Rust, ~549k LOC, 120.9k stars; sandbox: Seatbelt/Landlock+seccomp+bwrap at codex-rs/linux-sandbox/src/landlock.rs; `codex --oss` Ollama/LM Studio; 14,901 open issues); pi (earendil-works/pi, TS, 100.9k stars) 4 default tools read/write/edit/bash, pi-agent-core/pi-tui/pi-coding-agent split; crush (Go, 27.9k); aider stale (last push 2026-05-22); nanobot (Go); OpenHands heavy. Zed → ACP (JSON-RPC over stdio, LSP-like; ZeroClaw exposes ACP). Claude Code: single-threaded flat loop.
- Steal ranked: ZeroClaw Signal shape; goose recipe+cron; codex sandbox layering; pi 4-tool default; Claude Code flat loop; ACP.
- Signal: NO official bot API; ToS forbids automated accounts. signal-cli (Java, JRE 25, v0.14.7 2026-08-01, GPLv3; Linux GraalVM native image 109.7 MB experimental; native libsignal NOT bundled for aarch64 → exquo/signal-libs-build libsignal_v0.101.2 2026-08-28, swap .so in jar, glibc 2.30+). presage (Rust, git-only, AGPL, 267 stars, libsignal 0.99). signal-cli-rest-api (Go wrapper, v0.100, polling default). signald DEAD. signalmeow (Go, mautrix). Rate limits harsher in 2026 (#1911 open: limits after 10 msgs/account). Rules: link as secondary device (`signal-cli link -n`), registering de-auths phone app; low volume; allowlist. Recommend signal-cli daemon HTTP for Rust AND Bun.
- Local runtimes: LM Studio native /api/v1 (0.4.0) w/ MCP, two tool tiers: native (hammer badge) vs default (injects [TOOL_REQUEST]...[END_TOOL_REQUEST] prompt + converts tool role→user) — the fallback shape to copy. Ollama tools API w/ streaming; no compat matrix; templates without .Tools silently ignore tools. llama.cpp --jinja default; Generic handler fallback; GET /props chat_template_caps.
- τ²-bench airline 2026-09-02: Qwen3.5 397B 78.7; Gemma 4 31B 76.1; Qwen3.5 35B-A3B 73.3; Gemma 4 26B-A4B 68.3; **Qwen3.5 9B 67.5**; gpt-oss-120b 64.4; gpt-oss-20b 51.4; Qwen3 14B/32B 42; Llama 3.1 8B 30.9; Qwen2.5 7B 16.7. → one generation > 3× params. Recommend Qwen3.5 9B/4B; Gemma 4 12B/E4B; Granite 4.1 8B/3B. Gemma 3 has NO native tools; Gemma 4 uses custom token DSL not JSON. BFCL secondhand numbers unreliable.
- Mac GPU ceiling ~70–75% of RAM (sysctl iogpu.wired_limit_mb). Pi 5: Qwen3-30B-A3B Q3 pp ~8.6–10.9 t/s, tg 8 t/s → prefill of 3k-token prompt = minutes/turn; "Pi cannot host the main model" → daemon on Pi, model on Mac/cloud via Tailscale.
- Fallback ladder: (1) fix template; (2) json_schema; (3) constrained decoding SPARINGLY — "Constraint Tax" arXiv 2605.26128: hard schema decoding validity 61.5→100% but accuracy 19.7→11%; calendar task 91.5% prompt-only vs 48% hard schema; (4) if leaving native, use JSON/Python not XML (BFCL V4 format study; XML worst for small models; "Bitter Lesson of Tool Calling" arXiv 2608.06370); (5) repair loops w/ admissible alternatives: +44pp (arXiv 2607.14167); HarnessFix +11.1%.
- Advice: read ZeroClaw first; signal-cli daemon; Pi hosts daemon not model; Qwen3.5 9B; ladder order native→json_schema→text protocol→repair.

## NAME: the new agent is "Coeus" (user chose; Titan of intellect / axis of heavens). Note ~/Code/coeus (Python/C++ realtime, "COEUS Greek Titan of Resolve, Intelligence, Query & Questioning") and ~/Code/coeus-app (Electron) already exist = JT's earlier project with same name.

## 09 Security & reliability (done)
- Threat table T1–T8: sender auth; web UI reached by wrong tailnet device; prompt injection via content; tool blast radius; secrets (browser profile = credential); supply chain (ClawHub); lateral movement over Tailscale; self-update/self-modification.
- Hermes SECURITY.md:60-64 "The only security boundary against an adversarial LLM is the operating system."
- Pairing: copy Hermes constants (8-char codes, 32-char alphabet, secrets.choice, 1h expiry, 3 pending, 1 req/user/10min, lockout after 5, hashed+compare_digest, 0600, never logged). OpenClaw dmPolicy default pairing; schema refuses open w/o "*"; pending expire 1h cap 3; reason codes. Store allowlist as Signal UUIDs. Web UI: bind tailscale0, `tailscale whois` per request + session cookie.
- Permissions: OpenCode engine (last-match wins, default ask, deny cascades, always appends rule; arity table reduces shell cmd to prefix). OpenClaw 8 intersected layers; subagents never get gateway/message tools; exec mode deny|allowlist|ask|auto|full. Hermes: 12 hardline regex (unapprovable) + 47 dangerous; file_safety deny paths; self_repo_guard; Tirith scanner; YOLO frozen at import; unattended surfaces fail closed (approvals.unattended_mode default deny); DON'T copy LLM auto-approval (:3641).
- Proposed 3-tier policy.yaml: tool classes read/write/exec/network/irreversible; default ask; allow/ask/deny pattern lists; irreversible always_ask + tainted_turn: deny + standing_approvals scoped to skill+action+limits+expiry+dry_run_first; Signal approval grants once or 1h never always for irreversible; cron unattended → ask fails closed w/ Signal message.
- Taint: OpenClaw provenance only (resultContentSource:"network"; turnTainted resets on user msg; consumers = memory classification); NO path blocks tool on taint → add enforcement. Copy external-content.ts wrapping (<<<EXTERNAL_UNTRUSTED_CONTENT id=random16hex>>>, notice, strip special tokens). Tool results carry source owner/local/network/email/social.
- Sandbox: OpenClaw docker off by default; no Seatbelt/bwrap in OpenClaw core or OpenCode. Recommend: Mac sandbox-exec Seatbelt profile from policy; Linux/Pi bwrap --unshare-all + Landlock + seccomp (ptrace/mount/keyctl), fallback systemd-run ProtectHome; daemon as own OS user `agent`; sandbox mandatory not a flag.
- Egress: net-policy pinned DNS lookup (resolve once, check all, dial checked) + block private ranges; add per-tool host allowlist; sandboxed children no network by default.
- Secrets: OpenClaw SecretRef {env,file,exec}; 1Password broker w/ grants + audit; Hermes strips borrowed creds, O_EXCL 0600; OpenCode plain auth.json 0600. Recommend: Mac keychain; Linux/Pi age-encrypted file w/ 0600 key; config holds secret:// refs; signal-cli data + browser profile 0700 excluded from sandbox mounts and backups; redact secret-shaped strings from logs.
- Browser profile: OpenClaw <CONFIG_DIR>/browser/<profile>/user-data; CDP 127.0.0.1; stale Singleton locks removed; reset → trash. Rules: one profile per persona ~/.agent/browser/<name> 0700; CDP loopback + per-launch token; on sandbox deny list; browser.post/submit = irreversible; screenshots = external content.
- Audit: OpenClaw kinds agent_run|tool_action|message; queue cap 4,096; hourly prune; pseudonymized. Rule: SQLite append-only rows for inbound msg, tool call, permission decision (+rule), approval reply, irreversible action (+payload hash); if queue full, tool waits.
- Reliability table (20 rows): launchd plist RunAtLoad/KeepAlive/ThrottleInterval 10s/Umask 077; systemd Restart=always RestartSec=5 StartLimitBurst=5/60s KillMode=control-group + ADD Type=notify WatchdogSec=60; sd_notify READY/WATCHDOG/STOPPING from main loop only; exit codes 75=restart me / 78=fatal config (RestartPreventExitStatus=78); readiness = ro DB open + config + disk + signal socket; post-restart /readyz gate; startup watchdog 300s armed before any I/O w/ migration leases; loop-liveness OS thread 30s probe/10s timeout/3 strikes → os._exit + heartbeat file; shutdown watchdog drain+60s; crash-loop breaker (3 in 300s → skip auto-resume, fail open; also pause cron job that crashed daemon twice); per-turn wall clock 15 min, per-tool 7 min; stall notify once; turn lease per session fail-closed; HARD CAP 50 tool calls/turn; delivery ledger pending→attempting→delivered/failed, dead-owner resend w/ "may be duplicate", 3 attempts/24h; lifecycle sentinel + quick_check after unclean exit + weekly integrity_check; corrupt DB → rename .corrupt-<ts>, restore nightly backup, degraded, tell owner; every queue capped (inbound backlog 100); drain marker w/ boot id + 30 min max age; shutdown flush to pending_messages/*.json w/ fsync; orphan cleanup ExecStopPost kill cgroup / Mac process group.
- Self-fixing tiers: T0 restart; T1 state repair (most value); T2 auto-rollback (only possible w/ single binary; OpenClaw only rolls back git checkouts); T3 model-assisted diagnosis w/ read-only tools + fixed repair menu + always ask (OpenClaw `openclaw triage` = T3 w/ full access; narrower); T4 model edits own code: only in worktree, CI tests, canary on Linux box never Pi w/ test Signal group, automatic rollback w/o model, human approves merge over Signal; no standing approval. Why: Prime Factorio cheating; Hermes gates memory/skill writes behind approval store (write_approval.py) because reviewer saved "wrong assumptions".
- Update strategy table: static binary per platform; self-installing `agentd update`; semver + signed manifest.json w/ SHA-256; download to releases/<v>/agentd.partial, verify sig w/ key baked in binary, fsync; rename(2) + symlink flip; keep last 3; unit written once at install pointing to ~/.agent/current/agentd (0644/0755); restart via systemctl/launchctl kickstart, READY=1 + /readyz within 60s; ExecStartPre auto-rollback if /readyz fails or crash-loop within 10 min of update, mark bad in releases/state.json; forward-only migrations w/ backup, older binary refuses newer schema and names version; signal-cli + Chromium external pinned, checked at /readyz; daily manifest check, install only in maintenance window w/ drain marker, model never calls update. Keep OpenClaw habit: refuse loudly, change nothing.

## 11 Browser/desktop/skills/outreach (done)
- OpenClaw browser ext (~145k incl tests; src 13.8k): launches real Chrome w/ --remote-debugging-port + --user-data-dir + hardening flags (--no-first-run --disable-sync --password-store=basic --use-mock-keychain on macOS; --headless=new when headless) (chrome.ts:763-800); profile <CONFIG_DIR>/browser/<profile>/user-data = JT's 358 MB dir; re-attaches by matching flags; playwright-core lazy + chromium.connectOverCDP (pw-session-cdp-transport.ts:234) w/ raw-CDP fallback; "user" profile via MV3 extension relaying chrome.debugger or chrome-devtools-mcp; remote target sandbox|host|node. ONE `browser` tool w/ 24 actions (doctor,status,start,stop,profiles,importprofile,tabs,open,focus,close,snapshot,screenshot,navigate,console,requests,errors,text,emulate,pdf,download,waitfordownload,upload,dialog,act) + 14 act kinds; aria/ai snapshots w/ refs; limits 100 actions/batch, 8s interaction timeout. Logins by hand ("Do not give the model your credentials"); importprofile cookies domain-scoped; NO captcha/2FA code → prompt says stop and ask. Desktop: `computer` tool (Anthropic-style v2 actions) → paired node; macOS Swift app (556 files ~159k lines) via Peekaboo + CGEvent + ScreenCaptureKit; cua-computer plugin wraps @trycua/cua-driver; screenshots model-only.
- Hermes browser (browser_tool.py ~6.5k): shells out to Vercel agent-browser CLI (Rust, raw CDP, @eN refs); backends local Chromium/Lightpanda/Camoufox/cloud (Browser Use, Browserbase, Firecrawl); /browser connect to own Chrome on 9222; 12 browser_* tools; use_real_profile COPIES user's Chrome profile to sidestep Chrome ≥136 default-profile debugging block; anti-bot heuristics only; computer_use on cua-driver ("does NOT steal cursor"), som/vision/ax capture, approval per mutating action. OpenCode/Prime: no browser/desktop tools.
- Skills: Hermes SKILL.md agentskills.io-compatible (name ≤64, description ≤1024, references/, scripts/...), 58 bundled; index in prompt w/ descriptions cut at 60 chars; skill_view body on demand; skill_manage create/edit/patch; background review after every turn "Be ACTIVE — most sessions produce at least one skill update" (noise!); /learn <what>; .curator_ledger.jsonl w/ rollback; usage counters; hub regex scan. OpenClaw: SKILL.md + metadata.openclaw (emoji, os, requires.bins, install); 52 bundled + 4 custodian; catalog truncates at 220 chars as <available_skills> XML; workshop "experience review" → PROPOSALS, auto-apply only workshop-owned skills, user-authored → operator review; skill_workshop tool; weekly collection review. OpenCode/Claude Code: scan ~/.claude/skills, ~/.agents/skills, .opencode/skills; skill tool loads body after permission check.
- Proposal: ~/.agent/skills/<name>/{SKILL.md ≤6 KB, run.ts, test.ts, fixtures/, CHANGELOG.md}; frontmatter name/description ≤120/triggers/tools/permissions{browser_profile, domains, max_actions, irreversible steps, approval never|first|always, rate}/test/version/learned_from. Teach-me: "teach: post to X" → headed browser records (Playwright codegen --user-data-dir) → run.ts w/ 3 selectors per step (role+name → text → CSS) → test.ts dry-run to irreversible step + screenshot over Signal → v1 saved approval:first → recall via index; skill_run enforces permissions outside model; failures → proposals/ queue applied after JT approves + CHANGELOG + rollback. Don't run "always write something" reviewer.
- Why browser agents fail: DOM drift, screenshot ambiguity, login state, modals, rate cliffs, irreversibility; 78% WebArena → 22% live; Online-Mind2Web general models ~40%; anti-bot fingerprints CDP/TLS; Playwright Chromium ≠ real Chrome; Chrome 136+ blocks default-profile debugging; LinkedIn bans (HeyReach founder banned 2026-03-25); Reddit crackdown; 2FA → hand off.
- Ladder: (1) official API/CLI (xurl, Meta Graph, Bluesky AT, Mastodon, YouTube, Gmail API, gh, himalaya); (2) scripted headed browser on persistent profile, recorded as skill w/ selector fallbacks, real Chrome, home IP; (3) vision computer-use only for one step w/ approval.
- Platform table: X free tier ended 2026-02-06, pay-per-use ~$0.015/post ($0.20 w/ link); LinkedIn partner-only API, HIGH ban risk; IG/FB Graph API needs App Review 2–4 wks; Threads API; Bluesky bots welcome (self-label), Mastodon low risk; Reddit high risk; TikTok audit; YouTube quota; Postiz (AGPL self-host, API + MCP, 20+ networks) on the Pi = pragmatic; Buffer new OAuth closed; Gmail API 2,000 recipients/day Workspace, SPF/DKIM/DMARC, List-Unsubscribe.
- Rolodex schema: contacts (email UNIQUE NOCASE, status, do_not_contact, consent_note, last_contacted_at...), campaigns (daily_cap, send_window), drafts (UNIQUE contact+campaign, approved_by), sends (draft_id UNIQUE, gmail_message_id, thread_id; insert `sending` BEFORE API call = idempotent ledger), events, suppressions. Mustache templates; model writes only 1–2 sentence personalization; dedupe normalize email; warm-up 10→40/day, jitter 3–15 min, recipient business hours; Signal approval "5 drafts ready" → `send 1,2,5`; Gmail API over SMTP (thread ids, history.list bounces). CAN-SPAM: headers, honest subject, postal address, opt-out honored ≤10 business days, up to $53,088/email; List-Unsubscribe; suppressions first.
- Other agents table (21): browser-use (Py, 106k), Stagehand (TS, v3 raw CDP, v4 extension; pushes Browserbase), Magnitude dormant, Skyvern heavy AGPL, Nanobrowser (extension), Claude in Chrome (product), Anthropic computer-use ref (tool computer_toolset_20260801), OpenAI Operator→ChatGPT Agent, Atlas discontinued 2026-07-09→"ChatGPT Work", Bytebot ARCHIVED 2026-03-07, UI-TARS stale, Cua/cua-driver (open-sourced 2026-04-23; ADOPT for desktop), Open Interpreter Rust rewrite v0.0.40 (watch), Agent S research, Magentic-UI/UFO research, Mariner shut 2026-05-04, Manus independent again, Comet product, agent-browser (Vercel, Rust+TS, 41.8k, Apache-2.0, --profile, linux-arm64) = BEST small engine, ZeroClaw browser depth unknown, nanobot (HKUDS Py 47.7k) no real browser, PicoClaw too thin, IronClaw no browser.
- "Claw desktop" = OpenClaw macOS app, Swift, Peekaboo default.
- Recommendation: 11 tools (browser_open/snapshot/act/screenshot/eval/file/tabs/handoff, computer_screenshot+act, skill_run, email_send); playwright-core connectOverCDP to self-launched real Chrome (or launchPersistentContext channel:'chrome'), ariaSnapshot + getByRole, codegen for teach-me; alt: agent-browser subprocess (Hermes pattern); Playwright under Bun not verified → Node worker if needed. Persistent profile per persona 0700, real Chrome binary, headed w/ window offscreen (--window-position=-2000,0), login by hand via browser_handoff, CDP loopback random port, nightly tar, doctor "logged in?" check. Pi: don't fight anti-bot; Mac runs Chrome worker exposing CDP/WS bridge over Tailscale w/ token; Pi proxies browser_* there ("browser host offline" → queue); Pi local headless Chromium only for public reads + Postiz + email ledger + cron + Signal.

## USER DESIGN GOALS (2026-09-02, verbatim intent)
- Analyze OpenCode's doom-loop functionality — JT believes it is what lets OpenCode work well with small local models. Verify and explain.
- Nerd Genie must be: fast, lightweight, reliable; ALWAYS minimal tokens; optimizes prompt caching; always speaks in simple, easy-to-understand English, not wordy.
- /commands must work in Signal AND terminal alike (like all these agents) — but only the most important ones.
- Guiding idea: simplicity of design and reliability.
- Scope: research + proposal only. No building yet.

## 12 Loop comparison (done)
- Drift since Aug: Hermes run_conversation ~7,250 lines, verify-on-stop off; OpenClaw agent-core 6,944 lines + Dreaming consolidator ON by default (writes MEMORY.md on schedule); Prime RLM depth 2, /refine auto on turn interval + on compaction w/ LLM review gate.
- Topology: OpenClaw one gateway daemon, subagents = sessions in-process, WS clients; Hermes CLI one proc, gateway separate proc (26 platforms), TUI Node via WS JSON-RPC, review = daemon thread, delegation = thread pool; Prime TS host + one Python child per session (stdin/stdout), daemon over Unix socket; OpenCode one server proc (Effect) + HTTP API, TUI/desktop/web separate clients, AI SDK, subagent = child session in-process.
- Entry: OpenClaw returns {runId} at once, per-session lane, activeWriterRunId fence, queue mode default steer, steering drained at tool checkpoints, skipped calls get synthetic error results; Hermes gateway busy_input_mode default INTERRUPT (queue/steer options), /steer appended to newest tool result; Prime two queues, steer if streaming else followUp; OpenCode one Runner/session, re-reads message list each step, shell gets BusyError.
- Context: OpenClaw prompt = base + skills + memory + 6 bootstrap files (20k/60k caps, USER.md 4k), cache boundary, Anthropic cache_control on system+tools; Hermes three tiers stable/context/volatile (MEMORY.md 2,200 chars, USER.md 1,375), rebuilt only on compression, FOUR cache breakpoints (static prefix, end of system, last two non-system msgs); Prime base RLM prompt + AGENTS.md + skills + harness state appended; OpenCode provider-specific base prompt + env + skills + instructions (AGENTS.md/CLAUDE.md/CONTEXT.md) RE-READ FROM DISK EVERY STEP.
- Model call: OpenClaw streams, 34 providers, tool-call-repair (3,564 lines: text JSON, XML-ish, Harmony markers → real tool blocks); Hermes NON-STREAMING by default, 38 providers over 4 transports, NO repair (malformed args → error result); Prime streams, partial-json repair; OpenCode AI SDK streamText, no repair layer, invalid tool for unknown names.
- Tool exec: OpenClaw parallel unless sequential flagged, commit in source order, beforeToolBatch admission, result cap 16k chars (32k/64k for big ctx, ≤30% ctx), exec timeout 1,800s; Hermes parallel/sequential segments preserving order (12 read-only parallel-safe, path overlap), 8 workers, 420s, terminal 180s default, output 50,000 bytes 40% head/60% tail spilled to file, file read 100k chars, errors {"error":...}; Prime one tool, cell cap 65,536, NO gate; OpenCode AI SDK runs tools in stream, unbounded concurrency, doom loop = 3 identical consecutive → permission.ask("doom_loop"), truncate 2,000 lines/50 KiB spill 7 days, read 50 KB/2,000-char lines, grep 100 rows, shell 2 min, every tool calls ctx.ask.
- Termination: OpenClaw NO turn cap, 48h run timeout, loop detector ends run on 2nd critical, outer 32–160 attempts; Hermes max_iterations = sys.maxsize, shared iteration_budget parent/children, outer exception cap 8, wall-clock wrap-up at 80%; Prime no cap, goal token budget, autonomous maxContinuations; OpenCode exits when finish ≠ tool-calls, agent.steps default Infinity.
- Overflow: OpenClaw proactive threshold, routes compact_only/truncate_tool_results_only/compact_then_truncate, max 3 attempts, keeps 20k recent tokens, never splits call/result, SILENT MEMORY FLUSH turn before compaction; Hermes 50% trigger (75% small windows), prune old tool results → protect head → protect tail → summarize middle, protect_last_n 20, summary target 20%, lean tail 6 tool rounds, REAL USER MESSAGES QUOTED VERBATIM (24k chars total, 4k each), recovery footer; Prime contextWindow-16,384, keep 20k, cut at turn boundary; OpenCode usable = input - min(20k, maxOutput), compaction agent, preserve_recent clamp(25%, 2k–15k), prune tool outputs older than 40k tokens when >20k prunable.
- Retries: OpenClaw outer attempt loop, transient 3, provider maxRetries 3, auth rotation, key rotation, model fallback, empty-error resubmission; Hermes api_max_retries 3, jittered adaptive backoff, error classifier → FailoverReason, provider fallback, credential pool; Prime 3 retries 2s exp, auth-failure rotation, NO model failover; OpenCode 5 retries 2s ×2 25% jitter 30s cap, honors retry-after, never retry overflow, NO failover.
- Delegation: OpenClaw sessions_spawn non-blocking, depth 1, 5 children/agent, 8 concurrent, results as steering items 6k chars each / 24k merged w/ "treat as data" header; Hermes delegate_task depth 1, 10 concurrent, sync/async durable in SQLite, parent sees summary; Prime await rlm() non-blocking, depth 2; OpenCode task tool blocking by default, background=true, depth 1, resumable by task_id.
- Learning table: OpenClaw bootstrap files (on), memory-core FTS5+vector tool (on), Dreaming (on), active-memory before_prompt_build injection (on), memory-lancedb (off); Hermes memory tool (on), background review fork every 10 iterations w/o writes (on; budget 16 iterations), /refine, /learn, curator (consolidation off), session_search FTS5; Prime harness_state.json via /refine + auto-refine LLM-gated (on) + rlm.harness.* , kernel-state.dill; OpenCode NONE automatic (AGENTS.md by hand or /init, skills from disk).
- LOC ratio loop:wrapper — OpenClaw 6,944 : 404,920 (~1:58); Hermes ~7,250-line function, agent/ 163k; Prime 2,326 : 123,871 (~1:53); OpenCode ~1,000 : 75,929 (~1:75).
- Recommended 15-step loop: ack w/ runId + writer claim; default steer at tool boundaries (skip unstarted w/ synthetic error); three-tier prompt w/ caps + cache breakpoints at end of stable tier and end of system; re-read context files every step; stream via one provider abstraction + text tool-call repair + args validation → error result; admit batch (beforeToolBatch) + doom-loop → question; parallel/sequential segments by path overlap, commit in order; one permission fn on every tool (last-match-wins, default ask, always appends); bound every tool (2 min shell, truncate+spill); taint network turns → tighten gate; one serialized post-turn checkpoint (budgets, refine, compaction); compact at ~50%, token-budgeted tail w/ whole tool rounds, verbatim user quotes, never split call/result, silent memory flush first; retries outside loop (3–5 jittered, honor retry-after, never on overflow; auth/failover in outer loop w/ budget); non-blocking delegation → steering items "data not instructions", depth 1; finalizer decides review fork.
- Learning adopt: MEMORY.md 2,200 + USER.md 1,375 char caps in volatile tier; background review fork w/ hard budget; harness-state notes w/ history + rollback + cheap review model; FTS5 session search tool; agent-authored SKILL.md + /learn; silent memory flush before compaction. Skip v1: vector memory, Dreaming, pre-reply recall, curator consolidation, dashboards. ONE RULE: every mechanism needs a read path (file read at prompt build or a tool) else it's logging not learning.

## 10 Verified metrics (done) — USE THESE NUMBERS
| Metric | OpenClaw | Hermes | Prime | OpenCode |
| Hand-written non-test lines | 2,937,809 | 1,485,870 (Py 1,055,438 + TS 469,222) | 171,389 | 341,520 |
| Generated/vendored inflation | 0.2% (6,251) | 2.5% (i18n) | 11.4% (models.generated.ts 22,086) | 29.9% (107,756 i18n + 33,361 OpenAPI codegen) |
| Non-test files / mean lines per file | 14,381 / 204 | 3,099 / 485 | 473 / 363 | 2,448 / 145 |
| Largest hand-written file | 2,986 (flat!) | 34,561 (gateway/run.py) | 11,948 | 3,465 |
| Whole repo tokei lines | 10,308,047 | 3,381,458 | 422,363 | 1,401,097 |
| Repo on disk incl .git | 5.0 GB (.git 4.5 GB) | 1.3 GB | 105 MB | 704 MB |
| Test files / cases / lines | 11,357 / 131,487 / 4,791,097 (1.6:1) | 3,759 py + 1,086 ts / 39,067 + 10,961 / 1.0M | 451 / 6,020 / 171,556 | 736 / 6,076 / 175,901 (0.4:1) |
| Root deps / dev | 65 / 60 | 34 / 9 (+44 optional groups, 103 optional) | 2 / 10 | 6 / 12 |
| Unique direct deps all workspaces | 192 (+81 dev) | 34 py + 103 py-opt + 103 ts | 37 | 220 (+107 dev) |
| Lockfile packages | 1,653 | 255 (uv) | 438 | 3,233 |
| Commits last 30d / 7d | 10,574 / 3,775 | 6,718 / 1,777 | 191 / 57 | 354 / 55 |
| Authors 30d / all-time | 384 / 3,184 | 923 / 3,238 | 15 / 247 | 43 / 1,024 |
| Total commits / first commit / days | 86,752 / 2025-11-24 / 282 | 27,319 / 2025-07-22 / 407 | 4,622 / 2025-08-09 / 389 | 15,636 / 2025-03-21 / 530 |
| Lifetime commits/day; last-30d/day | 308; 352 | 67; 224 | 12; 6.4 | 29; 12 |
| Top author 30d | Peter Steinberger 6,689 = 63% = 223/day (agent-driven dev) | Teknium 1,940 = 29% | Sebastian Müller 120 = 63% | opencode-agent[bot] 133 = 38% |
| Tools exposed | 57 (CORE_TOOL_DEFINITIONS in src/agents/tool-catalog.ts; 14 groups) | 83 in 59 toolsets | 1 (ipython) | 12 + 4 conditional |
| Channels | 27 | 31 (8 gateway/platforms + 22 plugin platforms + ...) | 0 | 0 |
| Providers | 57 ext / 76 ids | 41 | 32 (1,238-model catalog) | 32 + models.dev |
| Config top-level keys / total paths | 42 / 10,175 (own generated baseline: core 2,417, channel 3,706, plugin 4,052) | 24 / ~406 distinct | n/m | 24 / ~90 leaf (est) |
| TODO / FIXME / HACK | 353 / 0 / 0 | 23 / 0 / 0 | 0 / 0 / 0 | 86 / 0 / 0 |
- OpenClaw tool ids (55): agents_list agents_wait apply_patch ask_user browser canvas code_execution computer conversations_* create_goal dashboard dismiss_task edit exec gateway get_goal github_* heartbeat_respond image_generate memory_get memory_search message mobile_ui music_generate nodes portal process progress_card read screen secrets session_status sessions* sessions_spawn sessions_yield show_widget skill_workshop subagents suggest_task terminal tts update_goal video_generate view_image web_fetch web_search write x_search.
- OpenClaw channels (27): a2a buzz clickclack discord feishu googlechat imessage irc line matrix mattermost msteams nextcloud-talk nostr qa-channel raft reef signal slack sms synology-chat telegram tlon twitch whatsapp zalo zalouser.
- Hermes run_conversation(): lines 1996–9240 = 7,245 lines, 13 params. OpenClaw: no file > 3,000 lines (flat); Hermes: extreme concentration.
- Runtime (Mac): OpenCode 137 MB binary, --version 0.26 s median, serve RSS 378 MB, clean shutdown. Others not runnable here (OpenClaw unbuilt, no Hermes venv, Prime not installed).
- Insight: OpenClaw's August = 63% of commits from one person at 223/day → agent-written code at a pace no human reviews; OpenCode's top committer is its own bot.

## 13 ZeroClaw vs Moltis (done) — verdict: BUILD FROM SCRATCH, BORROW MODULES
- ZeroClaw: 774,627 Rust code lines (~394k non-test; 57% tests), 22 lib crates, 1,274 locked crates, 140 unique direct deps, MSRV 1.96, edition 2024, release profile opt-level=z + fat LTO + panic=abort. Largest files: config schema.rs 43,402; channels orchestrator 35,762; agent/loop_.rs 17,898; sop/engine.rs 16,212; main.rs 12,465; providers/reliable.rs 10,631. v0.8.4 2026-08-02 (nothing since); arm64 linux gnu+musl tarballs ~27 MB; BUT `channel-signal`, `sandbox-landlock`, `sandbox-bubblewrap` NOT in default/dist features → must build from source. 373 commits/30d, 404 contributors, 506 open issues, 308 open PRs; memory subsystem mid-redesign (3 RFCs). anyhow everywhere; ~74 non-test unsafe; i18n Fluent keys required for every user string; 16,670 tests.
- ZeroClaw loop: run_tool_call_loop (turn/mod.rs:381); DEFAULT_MAX_TOOL_ITERATIONS = 10 → finish_after_max_iterations bounded final answer; parallel join_all (serial if approval needed or tool_search); malformed tool protocol fed back up to 2 retries; loop detector hashes recent calls (window 20, 3 repeats) → nudge; NO LLM summarization in loop — history_trim keeps whole recent turns within budget, on context error trims + retries once; DEFAULT_MAX_HISTORY_MESSAGES 50; streaming w/ fallback to non-streaming; HMAC-SHA256 tool receipts per session so model can't fake results.
- ZeroClaw Channel trait (zeroclaw-api/src/channel.rs:619): name, send, send_final, listen(mpsc), health_check, send_choice, start/stop_typing, add/remove_reaction, request_approval. Signal (signal.rs 2,135 lines, tests from :953): signal-cli daemon HTTP; JSON-RPC POST /api/v1/rpc 30s timeout; SSE GET /api/v1/events?account=; backoff 2→60s; allowlist exact E.164/UUID or "*"; group_ids + dm_only; group:<id> targets; attachments NOT downloaded (issue #7891); sendTyping; /api/v1/check; polls; reactions; approvals over Signal w/ 300s timeout.
- ZeroClaw cron: CronJob {Schedule Cron/At/Every, JobType Shell/Agent, SessionTarget Isolated/Main, DeliveryConfig, allowed_tools...}; SQLite jobs.db tables cron_jobs/cron_runs, output cap 16 KB; tick min 5s; claim lock via UPDATE ... WHERE locked_at IS NULL; missed-run skip/catch-up; retries w/ backoff 200ms→30s jitter; shell jobs 120s; agent jobs cannot call cron_* tools. SOP engine 30.6k lines (overkill).
- ZeroClaw memory: SQLite brain.db w/ embedding BLOB + FTS5 BM25; OpenAI-compatible embeddings only; recall pipeline budget 5 recalled/4 rendered/800 chars each/4,000 total wrapped [Memory context]; LLM consolidation. Role files AGENTS/SOUL/TOOLS/IDENTITY/USER/BOOTSTRAP/MEMORY.md injection (system_prompt.rs:27-43) "a 100-line idea". Skills: SKILL.md agentskills.io; SkillCreator::create_from_execution turns successful tool trace into SKILL.md via reflection prompt, dedup by embedding cosine, OFF by default, max 500; SkillImprover w/ cooldowns = best learnable-skills impl seen.
- ZeroClaw providers: ModelProvider trait; anthropic.rs 7.3k; compatible.rs (~20 vendors, 300s idle stream timeout); ollama.rs native /api/chat (num_ctx 8192, num_predict 2048, 600s, <think> strip); LM Studio = compat at localhost:1234/v1; ReliableModelProvider 10.6k lines (fallback map, key rotation on 429, retry-after, stream resume on next candidate). Tool-call parser crate (lib.rs 5.5k, standalone): 6 envelope shapes, nested JSON-in-string, XML-tag calls, malformed/incomplete detection, <eom>/think strip; text tool protocol injected for non-native models = most reusable file.
- ZeroClaw web: axum; WS chat w/ approval frames; SSE w/ replay ring; JSON-RPC 12.8k; React 19 + Vite + Tailwind SPA (33k TSX + 22k TS), embedded via include_dir under `embedded-web` feature (dist embedding not verified); pages Dashboard/Chat/Agents/Cron/Skills/Sops/Runs/Tools/Config/Logs/Doctor/Pairing/...; PairingGuard one-time code + SHA-256 bearer tokens + lockout; WebAuthn optional; `tailscale serve`/`funnel` shell-out.
- ZeroClaw security: autonomy ReadOnly/Supervised(default)/Full; approvals Yes/No/Always/ReplaceWith; shell allowlist + risk gate (detects unsafe redirects); sandboxes Landlock (pure Rust crate), Seatbelt (/usr/bin/sandbox-exec), bwrap, Firejail, Docker via detect::create_sandbox — NOT in prebuilt; secrets ChaCha20-Poly1305 w/ 0600 key + ciphertext-only config, 1Password optional; per-tool domain allowlists + SSRF guard; prompt-injection guard; signed audit log; emergency stop. Browser: facade over npm agent-browser CLI (Node required) / fantoccini WebDriver / computer_use HTTP sidecar (not in repo); no native persistent Chrome profile; desktop = screenshots only. Self-update: GitHub release, SHA256SUMS, .bak, swap, smoke test, rollback; NO signature check; service install systemd/launchd/Windows/OpenRC; /health + component registry w/ restart counts; channel restart backoff; stall watchdog; HEARTBEAT.md engine; doctor.
- Moltis: 406,823 Rust code lines, 75 crates, 1,376 locked crates, 175 unique deps, nightly-2026-06-20 pinned, gnu-only Linux (no musl/Windows), 63 MB tarballs, embeds llama.cpp + wasmtime + Swift bridges + voice + telephony; 1,500-line file cap enforced by CI; thiserror per crate; 50 unsafe; 7,754 tests; one maintainer (penso 3,600 commits vs next 61; "claude" 4th largest committer); AGENTS.md: "Rust version of openclaw"; 124 commits/30d, 62 open issues, near-daily releases; updater sha256 + GPG. Browser = chromiumoxide CDP, managed Chrome, persist_profile: true default, profile_dir, DOM snapshot w/ numbered refs (crates/browser 7.5k) = the one thing ZeroClaw lacks. Signal 1.7k same design (no attachments). Cron sqlx/JSON stores + heartbeat. Memory markdown→chunks→embeddings→SQLite hybrid; local embeddings via llama-cpp-2 FFI. No desktop automation, no contacts DB, email read-oriented.
- Fit table: both ✅ Signal (no attachments; ZeroClaw source build), web UI, cron, role memory, LM Studio/Ollama, Anthropic; ZeroClaw ✅ skills auto-creation; Moltis ✅ persistent-profile browser; both ❌ desktop automation (ZC screenshots only), ❌ contacts DB; ZC ❌ easy for Claude to maintain (775k, god files, i18n); Moltis ⚠️ (bus factor 1, nightly).
- Recommendation: don't use as-is, don't fork; build from scratch and lift: Signal channel (~950 LOC + allowlist 40; replace zeroclaw_log w/ tracing; add getAttachment), Channel trait (~100), cron types/schedule/store/scheduler (~2,500), tool-call parser crate (~2,000; can depend directly), self-update + restart (~500; add minisign/Sigstore), sandbox wrappers landlock/seatbelt/traits/detect (~1,300), role files + memory inject pattern, skill creator (~1,500), Moltis CDP browser manager/detect/snapshot/types (~3,500), tailscale serve (~150). Write fresh: loop (~600 lines: 10-iteration, join_all, trim-on-overflow, HMAC receipts), provider layer (one trait; Anthropic native, OpenAI-compat, Ollama native), web UI (small axum + SSE page), contacts/outreach, desktop automation. Licensing clean (MIT/Apache; MIT; no presage/libsignal linked; signal-cli separate GPL process). Pin commit b7bc2b2; treat lifted code as owned. Lifted deps: tokio, reqwest(rustls), serde, rusqlite, cron, landlock, chromiumoxide, sha2, hmac. "Lightweight is marketing."

## 15 Tool inventory (done)
- Granularity: Prime 1 tool; OpenCode 17 eager w/ .txt descriptions; OpenClaw 57 behind profiles; ZeroClaw ~120 Rust tools; Hermes ~83 w/ 19 eager behind tool_search/tool_describe/tool_call (−49% schema tokens); Atomic names tools os.fs.read style + GBNF grammar array-only root; Codex ~6 (shell/unified_exec w/ yield_time_ms + PTY session ids, apply_patch freeform Lark grammar, update_plan, view_image, web_search, request_user_input, nine multi-agent tools; sandbox escalation = call field `sandbox_permissions` + `justification` shell_spec.rs:232-262; defer_loading on MCP tools); Claude Code ~30 (ToolSearch on by default ≤5 results auto:N, core exempt; Bash 2 min/10 min ceiling auto-background on timeout; Grep head_limit 250; WebFetch takes prompt → small-model answer; AskUserQuestion; Agent fork/worktree depth 3; CronCreate 7-day expiry + jitter; PreToolUse hook updatedInput = policy layer).
- Reading: everyone has offset/limit + line numbers except Prime; OpenCode 2,000 lines/2,000 chars per line/50 KB; Hermes 2,000 lines ~100k chars + next_offset; OpenClaw byte pages 32–128 KB.
- Editing: exact-match old/new everywhere; OpenCode adds 9 fallback replacers + size guard (edit.ts:694-710); patch envelopes for GPT family; read-before-edit enforced by Claude Code + OpenCode.
- Shell: OpenCode 2 min; Claude 2/10 min auto-background; Hermes 180s + background+notify; OpenClaw yieldMs 10s then auto-detach (bash-tools.schemas.ts:37-40); Codex yield_time_ms + PTY ids; Prime always-background handle; Atomic NO default timeout; ZeroClaw fixed + 1 MiB cap. Output caps 30K chars (Claude), 50 KB (OpenCode/Prime), 100K (Hermes), 10K tokens (Codex).
- Search: tools camp (Claude Glob/Grep; OpenCode 100-row; Hermes single search_files w/ target+output_mode) vs shell camp (Prime, OpenClaw). Web: all have fetch+search; Hermes web_extract 15,000-char budget; OpenCode 5 MB/120s. Planning/todo: UI not capability → skip. Ask user: structured options everywhere; Hermes A/B: clarify must stay EAGER (18/18 vs 7/18 tool_search.py:265-272). Delegation: OpenCode task (background, task_id resume), Hermes delegate_task (output_schema), Claude Agent+SendMessage. Memory: OpenClaw memory_search+get "mandatory recall step"; Hermes memory batch + session_search FTS5; ZeroClaw 5 memory_* w/ categories; Claude/Codex/OpenCode none. Skills: index in prompt, body on demand everywhere (Hermes 57-char trigger rule). Scheduling: OpenClaw cron 10 actions/4 kinds/one job schema; Hermes cronjob_manage 7 actions "list first, never guess ids". Messaging: OpenClaw message w/ dryRun + idempotency; ZeroClaw send_via/escalate/poll/deliver_file. Integrity: only ZeroClaw HMAC receipts; only OpenCode `invalid` repair tool.
- Nerd Genie 20 TOOLS: read, write, edit, search, shell (yield→background, job id, poll/log/kill, justification for escalation), web (mode search|fetch, question?, 15k chars), memory (search|get|save atomic batch), ask_user (eager; recommended first; Signal renders numbered list), message (out-of-turn sends/files; dry_run), schedule (one job object at|every|cron; list before update; never guess ids), delegate (background, wait_ids, output_schema, tools=none for plain LLM call), skill (view|run|save), tool_search (select:a,b or keyword, ≤5), browser_open (list|close), browser_snapshot (text @refs | image), browser_act (batch incl upload/download), browser_eval (off unless skill allows), browser_handoff, computer (screenshot|act; every act approval), email_send (approved drafts only). Perm classes R/W/X/N/I per tool.
- Token cost: ~150–220 tokens/tool → ~3.5–4.5K for all 20; ~2K for 13 core. Local models: eager 13 (read write edit search shell web memory ask_user message skill browser_open browser_snapshot browser_act), deferred 7 (schedule delegate browser_eval browser_handoff computer email_send + tool_search lists them) → trims ~1.5K/turn, keeps choices under the 30–50 accuracy cliff. Cloud gets all 20 eager.
- Non-tool mechanisms to copy: OpenCode invalid repair channel; ZeroClaw HMAC receipts; Claude PreToolUse updatedInput; Atomic GBNF array-only root for llama-server.

## SCOPE CHANGE (user, 2026-09-02 afternoon)
- Nerd Genie only needs to run on LINUX (rosie x86_64 primary; Jarvis Pi 5 arm64 optional). No macOS target for the daemon.
- English only. No i18n.
- Interfaces: Signal chat or a TUI. No web UI in v1 (drop from core; can be a later client since the API makes it cheap).
- Consequences: sandbox = bwrap + Landlock + seccomp only (no Seatbelt); secrets = age-encrypted file (no keychain); browser worker runs on rosie (has display, Chrome profiles, home IP) — not the Mac; terminal client is now a first-class surface (TUI), so the interface section is Signal + TUI sharing one command table; the Mac is only a dev machine + LM Studio optional.

## 14 Doom loop & tokens (done)
- OpenCode doom loop: DOOM_LOOP_THRESHOLD = 3 (processor.ts:29); key = tool name + JSON.stringify(input) over the LAST THREE PARTS of the assistant message (processor.ts:352-368); fires a `doom_loop` permission ask, default "ask" (agent.ts:121), shown as "Continue after repeated failures" w/ once/always/reject; allow injects nothing; reject → PermissionRejectedError ends turn, errored message dropped from history. FLAW: step-start/step-finish parts are written around every LLM step (processor.ts:426-468), so one-identical-call-per-step loops produce [step-finish, step-start, tool] and the `every(type==="tool")` test fails → detector only catches three identical PARALLEL calls in one response; no test for multi-step loops. Not verified at runtime. VERDICT: NOT what makes OpenCode work with small models.
- What actually helps OpenCode w/ small models: `invalid` tool turns malformed/unknown calls into normal error results (llm.ts:303-315); lowercase name repair; continue on finish reason `unknown` (prompt.ts:1113,1295; v1.18.21); 2,000-line/50 KB caps; compaction w/ 20k reserve + tool-output pruning; small model for titles; ~2,100-token default.txt prompt for non-branded models.
- Side-by-side: identical-call key — OpenCode name+JSON; OpenClaw name+sha256(stable JSON), warn 10/block 20/breaker 30; Hermes none; Prime none (budgets: 12 turns autonomous); ZeroClaw name+hash(canonical JSON) nudge 3/block 4/abort 5, cap 10 (4 on local_small preset), result caps 8k/4k/64k by preset, parser + 2 malformed retries. Cache points: OpenCode 2 system + last 2; OpenClaw boundary; Hermes 4 breakpoints; Prime system+last tool+last user.
- Fixed tokens/turn (estimates): OpenCode local ~6,500 (2,200 prompt + 4,300 tools); OpenClaw ~10–15k; Hermes ~8–15k (33 core tools); Prime ~3,500; ZeroClaw local_small must fit 8k total, ~1,500–3,000.
- Nerd Genie LOOP GUARD (5 parts): (1) identical-call detector: key = name + canonical JSON (sorted keys), counted ACROSS steps ignoring step markers, window 20; 3rd identical → don't execute, return "Same call as the last two. The result would be identical. Do something different or answer."; 4th → same + end turn w/ one-line report; also stop after 3 identical tool OUTPUTS in a row (ZeroClaw results_collect.rs:190-191); NEVER ask the user (they're on Signal, may be away). (2) malformed-call catcher: unknown name → lowercase, -/space→_, CamelCase→snake, strip _tool, fuzzy 0.7 (Hermes); still unknown → "Tool 'x' does not exist. Available: a, b, c."; empty name → "That was data, not a call."; bad JSON → validator message (OpenCode invalid); never abort turn. (3) text tool-call repair: parse <tool_call>{json}</tool_call>, ```json fences, Harmony markers; after 2 unparseable attempts tell the model once then accept text as answer (ZeroClaw MAX_MALFORMED=2). (4) per-turn tool cap: 8 local / 20 cloud; at cap one short user msg "Tool budget used. Answer in text: what you did, what is left." (5) every guard message ends w/ same 3 options: answer the user, ask one question, or use a different tool/arguments (OpenClaw agent-loop.ts:1578); finish reason unknown treated like tool-calls; retry empty output twice then stop; 10-min idle watchdog.
- TOKEN BUDGET: fixed prefix ≤ 2,000 tokens for local: identity+style ≤110, environment ≤60, 6–8 core tools w/ ≤40-word descriptions flat schemas ≤1,200, tool_search bridge listing deferred names ≤150, memory summary ≤300, skills names only. Dynamic tail after boundary: date/time, active jobs, last approvals. Tool result cap 4,000 chars local / 16,000 cloud, overflow to file "[truncated: N of M lines; full output: <path>]". Compaction at 50% local (75% if ctx ≤16k), reserve 4k output, prune old tool outputs first, summaries on small model.
- CACHE LAYOUT: [system: identity+style | env | core tools | tool_search manifest] ← stable, cache point A; <!-- NERDGENIE_CACHE_BOUNDARY -->; [system: date/time | memory delta | jobs] ← B; [messages ... last two] ← C, D. Anthropic four cache_control markers (Hermes layout). LM Studio/Ollama prefix caching is byte-exact → never change anything above boundary within a session; keep num_ctx fixed.
- STYLE SNIPPET (74 words): "You are Nerd Genie, JT's home assistant. Write plain, short English. Match length to the ask: one-line question, one-line answer. No filler, no restating the request, no narrating tool calls. State facts; say 'not sure' when unsure. Use a tool only when needed, and never repeat a call with the same arguments. When work is done, report three things: what changed, what you checked, what is left. Ask at most one question at a time." Tool-result rule: plain text; line 1 status `ok` / `ok (truncated)` / `error: <one sentence>`; then payload; truncated results end w/ path line; error results end w/ "Options: answer the user, ask one question, or try different arguments."; no JSON wrappers/markdown tables in results.
- COMMANDS (12): /new /sessions [id] /model [name] /compact /undo /status /help /stop /pause|/resume /jobs [id run] /approve <id> [always] /deny <id>. One registry {name, aliases, argsSchema, run(ctx)->string}; handler returns plain text; terminal prints, Signal sends. Approvals: core emits permission.asked w/ id; Signal: "Approve #3 (bash: rm -rf build)? /approve 3 or /deny 3". /exit terminal-only. (OpenCode actually has ~26 slash commands, not 31.)

## USER DECISION (2026-09-02): BROWSER IS CORE
- "we just need our agent to use a web browser like a human... we dont need apis for any of social media stuff... the agent to have its own browser profile and use it like a human... to use browser use or whatever."
- → Browser is a first-class core capability, not optional. Own persistent profile. Human-like use: navigate, see, click, type, scroll, upload/download, dialogs, tabs. Social media via the logged-in browser, not APIs. Drop the "API-first ladder" framing. Keep the human-likeness practices (real Chrome, headed, home IP, human pacing, JT logs in by hand once, handoff on 2FA/captcha, never type passwords, no cloud proxies) because that IS how to use a browser like a human without getting banned.
- Engine: spawn-on-demand helper process (benchmark: importing playwright-core = +85 MB / +135 ms). Candidates: (1) Vercel agent-browser — single Rust binary, raw CDP, aria snapshot w/ @refs, --profile persistent, linux-arm64, no Node; what Hermes ships. (2) playwright-core worker (Node/Bun) — richest primitives (auto-wait, getByRole, ariaSnapshot, codegen recorder). (3) chromedp/rod in-process if daemon is Go — one binary, write snapshot/ref logic (~1k lines). browser-use (Python) = a whole agent loop; not needed since Nerd Genie is the loop.
- Host: rosie (Linux Mint desktop, display, Chrome, 60 GB, Ollama) runs everything: daemon + browser + local model. Pi optional.

## 08 Language & runtime (done) — measured on M2 Max, medians of 5
- Hello world TTFO: C 1.83 ms / Zig 1.83 / Rust 1.73 (333 KB) / Go 2.26 (2.49 MB, 1.66 stripped) / Bun compiled 7.87 (59.9 MB) / bun run 8.0 / node 30.4 (119 MB runtime).
- Minimal daemon (HTTP + SQLite): Rust axum+rusqlite 2.96 MB, 2.90 ms, 3.7 MB idle, 17.2 s clean build, 0.97 s incremental; Go net/http+modernc sqlite 14.3 MB (9.66 stripped, 9.44 MB static arm64), 8.23 ms, 19.1 MB idle, 6.0 s clean, 0.11 s incremental; Bun compiled 59.9 MB (96.8 MB linux-arm64!), 11.0 ms, 22.5 MB; bun run 11.1 ms 25.1 MB; node 46.8 ms 59.6 MB.
- Realistic dep sets: Rust (239 crates: tokio, axum+ws, reqwest rustls, serde, rusqlite, croner, keyring, tracing, clap, tungstenite) 6.19 MB, 5.94 MB musl static, 9.7 MB idle, 24.5 s clean release / 12.8 debug, 1.27 s incremental, cross needs cargo-zigbuild (plain cargo fails on bundled sqlite headers). Go (66 modules incl. chromedp, official MCP SDK, anthropic-sdk-go, go-keyring, coder/websocket, robfig/cron, modernc sqlite) 23.5 MB, 23.3 MB static arm64, 19.4 MB idle, ~10 s clean, 0.14 s incremental, cross build 10 s w/ NO extra tools (CGO_ENABLED=0). Bun w/ playwright-core: 66.6 MB (103 MB linux), **107 MB idle**, needs --external chromium-bidi to compile on Bun 1.3.5.
- Rust friction hit today: cargo 1.84 too old for edition-2024 crates (needed resolver=3 workaround); croner 3.x API drift → extra fix cycle. Go/Bun hit nothing. Each Rust target dir ~650 MB.
- Playwright import cost: Bun daemon 11 ms/22.5 MB → 147.8 ms/106.8 MB with `import { chromium } from "playwright-core"`; node 240 ms/132 MB. → browser MUST be a lazily spawned helper in every architecture.
- Pi 5 build estimates (5–9× Mac): Go clean 1–2 min, incremental 1–3 s; Rust 15k-line app ~150 deps clean release 5–12 min, incremental release 1–4 min, debug 15–60 s; Bun none. Rust on Pi needs ~1 GB toolchain + 650 MB target dirs.
- Recommendation: (B) Go daemon + small Bun/TS browser worker spawned on demand over JSON-RPC stdio. Reasons: Claude's fix loop one round/seconds; boring mature runtime for weeks-long process; official Go SDKs for Anthropic, OpenAI, MCP, Gmail; pure-Go SQLite → trivial cross builds; 19 MB idle = 25× under OpenClaw. Runner-up: Bun single binary (daemon must not import Playwright; worker = same binary in worker mode) if one language outweighs runtime maturity; Playwright-on-Bun only official since Bun 1.4 (2026-08-20). Rust not for v1: 10 MB advantage buys nothing, cost extra fix cycles today. No embedded REPL in v1.
