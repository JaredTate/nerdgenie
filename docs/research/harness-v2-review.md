# Fact-check of /Users/jt/Desktop/HARNESS_V2.md

Reviewed 2026-09-02 against the 17 study files (00–16) in `scratchpad/research/`, `scratchpad/notes.md`, and the repos at `/Users/jt/Code/{openclaw,hermes-agent,prime-agent,opencode,zeroclaw}` (HEADs 752a983, 95f62ca, 0ba0423, 69c172e, 73ff732; ZeroClaw ext clone b7bc2b2). Every number, name, path, quote, table cell and diagram label was checked. Verdicts: WRONG (contradicted by ground truth), MISLEADING (sourced but the wording overstates or misattributes), UNSUPPORTED (no source found), CORRECT.

## A. WRONG and MISLEADING

| # | Location (section, table row or diagram) | Claim as written | Verdict | Correction | Source |
|---|---|---|---|---|---|
| 1 | Part 0, verdict table, Hermes row | "Its terminal UI is three processes joined by a pipe" | WRONG | Two processes by default: Node screen (`ui-tui`) and Python `tui_gateway.entry` over stdio. The third (`compute_host`) exists only when `dashboard.turn_isolation` is on, default `False`. Part 2.7 gets this right ("optional third process"). | 03-hermes.md §3.2; `hermes-agent/hermes_cli/config_defaults.py:1719` |
| 2 | Part 1 §2 Hermes diagram, node T | "83 tools, 19 shown, rest behind tool_search" | WRONG | Inverted. 18–19 tools are *deferred* behind `tool_search`/`tool_describe`/`tool_call`; the core set (~33 tools, `_HERMES_CORE_TOOLS`) is always shown. Write "83 tools, ~33 always shown, 18 deferred behind tool_search". | 14-doom-loop-and-tokens.md §3 Hermes ("33 core tools always exposed, 18 names deferred"); 15-tool-inventory.md §2; `hermes-agent/tools/tool_search.py:273-284` (18 names) |
| 3 | Part 5 scoreboard, Tools row, Hermes | "83 (19 shown)" | WRONG | Same as #2: "83 (18–19 deferred)". | same |
| 4 | Part 1 §7, Codex bullet | "Two tools, run a command and apply a patch" | WRONG | Codex CLI exposes many tool specs: `exec_command`, `write_stdin`, `request_permissions`, `apply_patch`, `update_plan`, `view_image`, `request_user_input`, `web_search`, eight multi-agent tools (`spawn_agent`, `wait_agent`, …), `tool_search`, `get_context_remaining`, `new_context_window`, three MCP-resource tools, plus plumbing. Notes summarize it as "~6" core plus nine multi-agent. Nothing in the current studies supports "two". | 15-tool-inventory.md §5; notes.md §15 |
| 5 | Part 2.1 table, "Idle memory", Hermes | "678 MB venv (+ Node)" | WRONG | That is disk footprint, not memory. Only the dashboard was running on the 5070 Ti machine (88 MB RSS, 2 days up); the gateway was not running, so Hermes idle RSS is *not measured*. | 00-5070ti-live-measurements.md lines 8, 18, 28 |
| 6 | Part 5 scoreboard, "Database tables", Hermes | "11" | WRONG | No study counts Hermes tables. The "11" appears to be lifted from study 01's "the ten a single-user Signal agent needs … 11-12 tables, not 160", which describes the proposed agent, not Hermes. Write "not measured" (repo: `hermes_state.py` 2 `CREATE TABLE`, `kanban_db.py` 15, `cron/executions.py` 1, `delivery_ledger.py` 1, plus dynamic). | 01-openclaw-architecture.md §9 (misread); 10-metrics.md (no Hermes table count) |
| 7 | Part 5 scoreboard, "Loop lines vs wrapper lines", ZeroClaw | "~1,000 in a 17,898-line file" | WRONG | No study measured ZeroClaw's loop at ~1,000 lines. The turn engine `run_tool_call_loop` lives in `agent/turn/mod.rs` split across ~25 files; `loop_.rs` (17,898 lines) holds the entry points. Study 13 calls the loop "correct but 30k lines" and says a fresh one would be ~600 lines. Write "~30,000 (agent/turn/*, loop_.rs 17,898)". | 13-zeroclaw-moltis.md §2.1, §5 |
| 8 | Part 1 §6 ZeroClaw table, "Language, speed" | "22 crates" | WRONG | Both checkouts (b7bc2b2 and 73ff732) have 20 directories under `crates/`. Study 13 wrote "22 library crates"; the repo says 20 (41 manifests in total including the root binary, 3 apps, `xtask`, firmware). | `ls /Users/jt/Code/zeroclaw/crates` and `scratchpad/ext/zeroclaw/crates` (20 each); 13 §1 |
| 9 | Part 1 §1 OpenClaw table, "Language, speed" | "Starting the gateway touches 41 percent of a 2.9-million-line codebase" | WRONG | 41 percent is 3,536 of the 8,703 non-test *files* in `src/`, and `src/` is ~1.9 M lines, not 2.9 M (2.94 M is src + extensions + packages). Write "statically imports 41 percent of the 8,703 files in src/ (about 1.9 million lines)". | 02-openclaw-failure-modes.md §4 row 1; 10-metrics.md §1 |
| 10 | Part 2.9 browser table, "Engine", Hermes | "Vercel's agent-browser CLI, real Chrome it launches" | WRONG (default) | By default agent-browser launches its own Chromium (Chrome for Testing). Hermes launches the *real* Chrome binary only when the consent flag `browser.use_real_profile` is on (then it copies the auth files and attaches with `--cdp`). Write "agent-browser CLI; its own Chromium by default, real Chrome only with use_real_profile". | 16-browser-agent-spec.md §2.1 and §4 (agent-browser "Default browser is Chrome for Testing") |
| 11 | Appendix, Method | "Sixteen detailed study files" | WRONG | Seventeen files, 00 through 16. | `scratchpad/research/` listing |
| 12 | Part 1 §4 OpenCode table, "The bad" | "The 'doom loop' detector only catches three identical calls made at the same time; one bad call per step slips past it" | MISLEADING | Sourced, but 14 §1.2 frames it as "By my reading … Not verified at runtime; a 10-minute test with a stub model would settle it." State it as a code-reading finding: "by code reading (not run), the check only sees three identical calls inside one model response (parallel calls), because step-start/step-finish parts sit between steps". | 14-doom-loop-and-tokens.md §1.2, §6 |
| 13 | Part 2.9, "What works better" paragraph | "the top scorers … share five things: text + screenshot, a 'did that work?' step, a different strategy on retry, a plan for long tasks, and cloud browsers with captcha solving" | MISLEADING | Study lists six; the omitted first item is "a frontier model with thinking on". Say "six things" or note the omission, since it changes the Nerd Genie trade-off ("keeps the first four, gives up the fifth" then becomes "keeps four of six"). | 16 §6 "What the leaders share" (1)–(6) |
| 14 | Part 2.5, "What works better" paragraph | OpenClaw's "every-turn recall and scheduled rewrites … caused two of its worst bugs this week (a 19 GB temp-file leak and a CPU-pegging reindex)" | MISLEADING | Both bugs are in memory-core's *reindex/lock* path (#136311 reindex lock + 19 GB orphan temp DBs; #136175 full reindex saturates CPU; #135347 reindex corrupts DB). No study attributes them to active-memory recall or to Dreaming. (#134993, the other CPU-peg, is a filesystem-discovery busy loop, unrelated to memory.) Write "the memory index rebuild caused two of its worst bugs this week". | 07-news-since-august.md §2 representative issues |
| 15 | Part 2.2 table, Hermes, "What stops a loop" | "a repetition guard" | MISLEADING | `repetition_guard.py` detects repeated *text fragments* in the truncated-response continuation path (#86581); it is not a tool-call loop detector. Study 14: "No identical-tool-call detector found" in Hermes. Write "wall-clock budget; no identical-call detector (only a repeated-text guard)". | 14 §3 Hermes; `hermes-agent/agent/repetition_guard.py:1-8` |
| 16 | Part 1 §2 Hermes table, "Language, speed" | "Python, 1.5 million hand-written lines" | MISLEADING | 1,485,870 is Python 1,055,438 + TypeScript 469,222 (desktop app, TUI, web). Write "1.5 million hand-written lines, 1.06 million of them Python". | 10-metrics.md §1 rows "breakdown" and "Hand-written non-test" |
| 17 | Part 1 §2 Hermes table, "The good" | "A watchdog on a plain thread that survived a 30-hour deadlock" | MISLEADING | The watchdog was *added because* a gateway sat deadlocked ~30 h with a live PID; it did not survive that incident. Write "added after a 30-hour deadlock". | 03-hermes.md §1 row 5, §8 (`hermes_startup_watchdog.py`) |
| 18 | Part 1 §2 Hermes table, "The bad" | "57 percent of 6,718 commits were fixes" | MISLEADING | 57 percent (3,680 fix commits) was computed over 6,440 commits in 28 days (study 03); 6,718 is the 30-day count from study 10. Use "57 percent of 6,440 commits in 28 days". | 03 §1; 10 §1 |
| 19 | Part 1 §1 OpenClaw table, "The bad"; Part 4 text | "317,000 lines of install, update, and doctor code" | MISLEADING | 317,099 = update-cli 9,273 + daemon 14,267 + *all* of `src/commands` 129,698 + *all* of `src/infra` 163,861. Doctor alone is 70,979; `infra` also holds net/proxy/TLS/process code. Study 02 calls it the "operations layer". Write "317,000 lines of operations code (CLI commands, infra, service install, updater)". | 02 §1.5 table |
| 20 | Part 1 §1 OpenClaw diagram, edge T → R | Repair node ("Text-to-tool-call repair for weak models") sits *after* tool execution | MISLEADING (diagram order) | `packages/tool-call-repair` promotes text into tool-call blocks in the model's stream, before the "Tool calls?" decision. Move R between M and K. Renders fine; the arrow is semantically wrong. | 12-loop-comparison.md row 4; 01 §2 "Tool-call repair" |
| 21 | Part 1 §6 ZeroClaw table, "Language, speed" | "a 43,000-line config file" | MISLEADING | It is `crates/zeroclaw-config/src/schema.rs`, the Rust config-schema source (43,402 lines), not a config file a user edits. Part 5 says "43,000-line schema", which is right. | 13 §1 top-10 files; `wc -l` confirms 43,402 |
| 22 | Part 0, verdict table, ZeroClaw row | "Signal, cron, web UI, local models in one binary" | MISLEADING | Only if built from source with `--features channel-signal`; `channel-signal`, `sandbox-landlock`, `sandbox-bubblewrap` are not in `default` or the dist feature list, and the browser tool needs npm `agent-browser` (Node) or chromedriver. The doc's own §6 table says so. Write "in one binary if you build it yourself". | 13 §2.2, §2.7, §2.8; `zeroclaw/Cargo.toml` features (`channel-signal` only in `channels-full`/`ci-all`) |
| 23 | Part 0, "The five things", item 2 | "OpenClaw has no maximum number of turns. Its only brake is a 48-hour timeout" | MISLEADING | Also the tool-loop detector (warn 10 / block 20 / breaker 30, run ends on a second critical loop) and the 32–160 outer attempt budget; the doc's own Part 2.2 lists them. Write "its brakes are a 48-hour timeout, a loop detector, and a retry budget; none is a turn cap". | 12 row 6; 01 §2 "Loop recovery budget" |
| 24 | Part 0 ZeroClaw row and Part 1 §6 "The bad" | "Borrow nine modules" / "Nine modules worth lifting" | MISLEADING | Study 13 §5 table has 10 rows, one of which is "pattern only" (role files + memory inject); its own summary says "borrow four or five small, well-tested modules from ZeroClaw … plus one pattern from Moltis". Pick one number and say what it counts. | 13 §0, §5 |
| 25 | Part 2.1 table, "Open issues", OpenCode | "~5,500" | MISLEADING | 5,584 is GitHub `open_issues_count`, which includes pull requests; the issues-only figure is "not verified". Mark it "~5,600 incl. PRs". | 07 §5 and footnote "*" in the quick-reference table |
| 26 | Part 2.7 table, OpenCode "Result" | "fast, no glitches" | MISLEADING | Study 07 cites #6835 "Desktop crashes when opening project after attach to remote server". "Fast, few glitches" or drop the absolute. | 07 §5 "Server / remote / attach" |
| 27 | Part 1 §5 Atomic Agent | "constrains every tool call with a grammar" | MISLEADING | The GBNF grammar (`grammars/tool-call.gbnf`) applies on the llama-server path (transport default "grammar"); cloud providers get JSON Schemas with `additionalProperties: false`. Write "constrains local-model tool calls with a grammar". | 15 §7 |
| 28 | Part 2.9 table, "Human pacing", OpenClaw | "no" | MISLEADING | OpenClaw has optional `slowly` typing (`locator.type(text, { delay: 75 })`, 75 ms/char) and an optional `delayMs` hover before click; it has no mouse-path pacing. Write "optional 75 ms/char typing; no mouse pacing". | 16 §1.3 and §5 "Stealth / human-likeness" |
| 29 | Part 2.9 table, "What the model sees", Stagehand | "Accessibility tree plus screenshot" | MISLEADING | Study: "Accessibility/XPath candidates via `observe`; DOM for `extract`"; vision "Optional". Write "candidate actions with XPath selectors from observe(); screenshot optional". | 16 §4 Stagehand; §5 rows "Page representation", "Vision use" |
| 30 | Part 2.9 table, "Waits after an action", Stagehand | "yes" | MISLEADING | Study cell reads "Playwright/CDP waits (not verified)". Write "not verified". | 16 §5 "Waiting" |
| 31 | Part 2.9 table, "Checks that it worked", Stagehand | "caches what worked" | MISLEADING | Study cell is "observe before act"; the observe→act cache is a replay optimisation (no LLM call on hit, "cache miss → LLM again"), not a verification step. | 16 §4, §5 "Verification after action", "Error recovery" |
| 32 | Part 2.9 table, "Captcha, 2FA", Hermes | "stop and ask, or its cloud" | MISLEADING | Hermes adds a `bot_detection_warning` suggesting delays or a Browserbase stealth plan, and pitches `browser_vision` "especially for CAPTCHAs"; study 11: "No 2FA handling". There is no explicit stop-and-ask rule (that is OpenClaw's SKILL.md). Write "warns and points at cloud stealth; vision tool for captchas; no 2FA handling". | 16 §2.4; 11 Part 1 Hermes "Anti-bot / 2FA" |
| 33 | Part 4 diagram, node D | "Updater checks ~/.config/systemd/user permissions: your dir is 775 (Ubuntu default group)" | MISLEADING | Study 02: "Which directory on JT's box: not verified"; the check tests the leaf or its nearest existing ancestor (any of `~`, `~/.config`, `~/.config/systemd`, `~/.config/systemd/user`), and the 775 story is the study's Debian/Ubuntu private-group explanation (the 5070 Ti machine runs Linux Mint 22.3). Label it "one of four dirs is group-writable, probably 775 from the Ubuntu private-group umask (not verified)". | 02 §1.1 row 1; 00 host line |
| 34 | Part 1 §4 OpenCode diagram node T and Part 5 "Tools" OpenCode | "17 tools" | MISLEADING | 17 entries in the `builtin` array, but that includes `invalid` (an error stub removed from `activeTools`) and four conditionals (`question`, `execute`, `lsp`, `plan`); 13–14 are visible in a normal session; study 10 counts "12 + 4 conditional". Write "17 defined, 13–14 visible". | 05 §5 ("17 defined; 13-14 visible"); 10 §6; 14 §2 (`invalid` hidden); `opencode/packages/opencode/src/tool/registry.ts:231-249` |
| 35 | Part 1 §6 ZeroClaw diagram node U; Part 5 "Chat channels" ZeroClaw | "30 channels" / "30" | MISLEADING (minor) | README says "30+"; `crates/zeroclaw-channels/src/` has ~36 channel modules. Write "30+". | `zeroclaw/README.md:30`; 06 Part B.1 |
| 36 | Part 2.6 table, Hermes "Sandbox" | none in-process; "the OS is the only boundary" (in quotation marks) | MISLEADING (minor) | Not verbatim. SECURITY.md:60-64 says "The only security boundary against an adversarial LLM is the operating system." Quote it or drop the quote marks. | 09-security-reliability.md §1 point 1 |
| 37 | Appendix, Method | "Line counts with tokei" | MISLEADING (minor) | The per-repo hand-written counts in study 10 are `wc -l` over `find`/`grep` file lists ("raw physical lines … `wc -l`"); tokei was used for whole-repo totals and for ZeroClaw/Moltis. | 10 §0; 13 §0 |
| 38 | Part 6, local-model table header | "Tool-use score (τ²-bench, 2 Sept)" | MISLEADING (minor) | These are the τ²-bench *airline* domain scores from OpenRouter's run; say "τ²-bench airline". | 06 Part D.2 |

## B. UNSUPPORTED

| # | Location | Claim as written | Verdict | Correction | Source |
|---|---|---|---|---|---|
| 39 | Part 1 §7, Claude Code bullet | "the model decides what to do, the harness decides what is allowed, and they never share a layer" | UNSUPPORTED | No study makes this claim. Study 06 only reports a "single-threaded main loop with one flat message list" from third-party write-ups (marked not verified); study 15 documents tools and hooks from the docs. Either cite the docs page or soften to "the docs describe permission as a hook layer outside the model". | 06 Part B.3 "Claude Code"; 15 §6 |
| 40 | Part 2.6 table, Codex "Sandbox" | "kernel sandbox, **always**" | UNSUPPORTED | The sandbox design is sourced (Seatbelt / Landlock+seccomp+bwrap, study 06), but "always" is not: study 09 says the Codex repo was not re-verified, study 15 read only the tool specs, and the call-level `sandbox_permissions`/`require_escalated` field implies escalation paths. The Appendix itself lists Codex internals as not verified. Drop "always" or mark it. | 06 B.3; 09 §2.4; 15 §5; Appendix "Not verified" |
| 41 | Part 2.8 table, Hermes row | "git pull plus pip" — rollback "none" | UNSUPPORTED | No study examined `hermes_cli/update_cmd.py` (11,217 lines). The repo shows `git pull` then `uv pip install -e .`, a `_run_pre_update_backup`, git rescue refs under `refs/hermes-update-backups/…`, and restore helpers for cron jobs and model config. So "none" is at least partly wrong; write "git pull + uv pip; pre-update backup and git rescue refs; binary-level rollback: not verified". | `hermes-agent/hermes_cli/update_cmd.py:10,166-168,486-543,601-646,680`; 03 §2 (size only) |
| 42 | Appendix, Method | "17 research agents, about 3.4 million tokens" | UNSUPPORTED | No file in the scratchpad records agent or token counts (`assigned.json`/`final.json` list tasks, not tokens). Keep only if you have the figure from the orchestrator log. | scratchpad listing |
| 43 | Part 3, rule 6 | "This matters ten times more with small local models" | UNSUPPORTED | Rhetorical multiplier; the studies give +44 pp from repair feedback and the constraint-tax numbers, not a 10× figure. Drop the number. | 06 Part D.5 |
| 44 | Part 5 scoreboard, "Database tables", ZeroClaw | "few" | UNSUPPORTED (vague) | Not counted in any study (brain.db `memories` + FTS index, `cron_jobs`, `cron_runs`, pairing tokens, audit …). Write "not counted". | 13 §2.3, §2.4 |

## C. Mermaid syntax

All seven `mermaid` blocks (Part 1 §1, §2, §3, §4, §6; Part 2.4; Part 4) use quoted node labels and quoted edge labels, so parentheses (`bash()`, `(Ubuntu default group)`), colons, `%`, `+`, `~/` and embedded single quotes all render. No unquoted labels, no bad arrows. Only issue is semantic (#20: the repair node is placed after tool execution).

## D. Count

Claims checked: about 415 (every number, name, path, quote, table cell and diagram label, plus the prose assertions with a checkable referent). Found CORRECT: about 370. WRONG: 11. MISLEADING: 27. UNSUPPORTED: 6.

Representative CORRECT items, spot-verified against the repos and not just the studies: OpenClaw 10/20/30 loop thresholds, 48 h timeout, 121 + 39 tables, 42 top-level config keys / 10,175 paths, 6,944-line `packages/agent-core` (1,776-line `agent-loop.ts`), 57 tools / 27 channels / 76 provider ids, 8 policy layers, 504 + 288 + 195 MB on the 5070 Ti machine, 10,574 commits / 63 % / 223 per day; Hermes 2,200 / 1,375 char caps, 50 % compression trigger, 12 hardline patterns, 7,245-line `run_conversation`, 17,898-line `cron/`, gateway default `interrupt`, 31 platforms, 41 providers; Prime 9 `registerApiProvider` calls, 2,326-line loop vs 123,871-line wrapper, single `ipython` tool, decoded Claude Code OAuth client id, 0 permission hits; OpenCode `DOOM_LOOP_THRESHOLD = 3`, `agent.steps ?? Infinity`, 20 tables, no sandbox/memory strings, 137 MB / 0.26 s / 378–403 MB, 3,233 packages, 341,520 lines; ZeroClaw `DEFAULT_MAX_TOOL_ITERATIONS = 10`, `MAX_MALFORMED_TOOL_PROTOCOL_RETRIES = 2`, 3/4/5 ladder (`max_repeats` 3, block at `> max`, break at `>= max+2`), `loop_.rs` 17,898, `schema.rs` 43,402, 506 open issues, 373 commits, 27 MB tarball, Signal and Landlock outside the dist features; language table (2.9 / 8.2 / 11 ms; 9.7 / 19 / 22 MB; 107 MB with Playwright; 6 / 23 / 103 MB; 25 / 10 / 0.3 s); τ² scores 67.5 / 42.0 / 30.9 / 16.7; 44-point repair gain and 91 → 48 constraint tax; the four arXiv ids.
