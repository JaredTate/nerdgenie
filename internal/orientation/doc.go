// Package orientation is the short block of facts a fresh window opens with:
// what is in the working folder, which ports are listening on this machine,
// and, when the task is not new, its newest results in full.
//
// Every fresh window used to cost rounds of finding bearings. On 6 September
// 2026 the model re-read about six earlier results by their ids after each of
// four serve restarts, because the fresh window carried only their one-line
// summaries, and at the start of tasks it listed the work folder and asked
// which ports were listening. Those are facts the harness can hand it up
// front, once, in the first message of the window, where they stay in the
// cached part of the conversation.
package orientation
