In the folder <WORK>, write index.html with a heading "Counter", a number shown as 0, and a button labelled "Count" that adds one to the number and sets window.count to the new value. Serve the folder on port 8123 with python3 -m http.server from <WORK>, open http://localhost:8123/index.html in the browser, click the Count button, and confirm with browser_read's ask field that window.count is 1. Tell me the answer the page gave.
